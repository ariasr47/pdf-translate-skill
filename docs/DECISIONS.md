# DECISIONS

Rulings on this repo's behaviour, scope or method, so they outlive the
session that made them. See `AGENTS.md` Rule 3.

A decision is never edited to reflect new information. If a later finding
reverses one, **append a new row** that says so and leave the old row in
place — the history of "we used to think X" is part of the record.

## Format

`date | decision | why | what would reverse it`

- **date** — ISO, the day the ruling was made (or measured, for a finding
  that hardens into a ruling).
- **decision** — the ruling itself, stated as something an agent can act on
  without re-deriving it.
- **why** — the evidence or reasoning, in enough detail that a skeptic could
  check it without you.
- **what would reverse it** — the specific observation that would make this
  wrong. Not "if we learn more" — the actual falsifying condition.

## Worked example

| date | decision | why | what would reverse it |
|---|---|---|---|
| 2026-09-15 | The glyph-count shaping tell (unshaped-vs-shaped glyph count from `verify.py`) is valid **only** for conjunct-forming Indic scripts, and only on probe strings that actually contain a merging cluster. It must never be used to claim a mark-stacking script (Thai, Lao, Khmer, Hebrew+niqqud) or Arabic is "verified shaped." | Measured across five probes, PyMuPDF 1.28.0, `tests/fonts/`: Devanagari क्षत्रिय 8→4 glyphs (0.50) and हिन्दी 6→5 (0.83) show the tell; कमल 3→3 (1.00) shapes correctly but is indistinguishable from broken output; Arabic مكتبة 5→**8** glyphs (1.60) — shaping *adds* glyphs, so the same test direction is backwards; Hebrew שָׁלוֹם+niqqud 7→7 (1.00) — mark stacking never changes glyph count. Full table and the `insert_text(fontfile=...)` font-fallback trap that produced two false positives: `AGENTS.md`. | A probe string in one of the excluded scripts that reliably changes glyph count under correct shaping (and stays constant under a known-broken render) would reopen the question for that script specifically — it would not restore the tell as a general rule. |

## Open ledger

| date | decision | why | what would reverse it |
|---|---|---|---|
| 2026-09-15 | Khmer **is** probed by glyph count, on the coeng cluster ខ្មែរ only; it is not a mark-stacking blind script. | Measured on this machine, PyMuPDF 1.28.2, Noto Sans Khmer: ខ្មែរ 5 → 4 glyphs (0.80); the 15 September brief's own table read the same (Khmer coeng 0.80). The worked-example row above listed Khmer among the blind scripts by analogy, not by measurement; `AGENTS.md`'s prose was corrected the same day and its measured table was not touched. | A Khmer face that shapes ខ្មែរ correctly yet draws 5 glyphs, or a GSUB-less render of it that still draws 4. |
| 2026-09-15 | Myanmar has a probe: သင်္ဘော (kinzi + stacked bha), 7 → 5. | Measured, Noto Sans Myanmar: သင်္ဘော 7 → 5 (0.71), မန္တလေး 7 → 6 (0.86). The word tested before, မြန်မာ, has no stack and reads 6 → 6 — uninformative, not a finding. | A Myanmar face that stacks correctly at 7 glyphs, or a GSUB-less render that still draws 5. |
| 2026-09-15 | Thai stays REVIEW; no probe. | ป้า 3 → 3; กำไร 4 → **5** — sara am decomposes under shaping, so a "shaped < naive" rule would fail correct Thai. Lao is treated the same by structure, unmeasured. | A Thai (or Lao) cluster that reliably loses glyphs under correct shaping and keeps them under a known-broken render. |
| 2026-09-15 | Gate 18 attests the **face**, not the document text. `prepare_font` adds the script's probe cluster to the subset and probes the subset it wrote; `verify` extracts every embedded font program on a page that draws a conjunct-forming script and re-probes it, once per (script, face). A face that lacks the probe glyphs is REVIEW, not PASS; blind and unmeasured scripts are REVIEW, never PASS; a Helvetica fallback on either arm is FAIL whatever the counts. | The document's own words may contain no merging cluster (कमल), so only a known probe has an expected count. MuPDF embeds the whole font program from `insert_htmlbox` (243,520 bytes in, 243,520 out, GSUB intact — measured), so the embedded face can be re-rendered. A pyftsubset subset keeps the Indic GSUB lookups for retained glyphs (subset probe 8 → 4, measured). Real run, red and green: `docs/reviews/2026-09-15-conjunct-shaping-gate.md`. | MuPDF starting to subset embedded fonts at save (the probe glyphs would vanish and every page would degrade to REVIEW), or a face that passes its probe yet draws a document conjunct broken — that would show the probe is not representative for that face. |
| 2026-09-15 | Skill version 48 → 49 (plugin 49.0.0, pyproject 49.0.0); `tests.test_shaping_probe` joins the CI suite line; the font cache key moves to `noto-fonts-v2`. | A new always-on gate changes what `verify` reports, so installed copies must be told; five new test faces must be cached, not re-fetched per run. | A ruling that gates do not bump the version. |
| 2026-09-15 | Gate 18 judges **every** embedded face on a page that draws a conjunct-forming script and covers the probe, not only the face that drew the visible run. A broken face that is embedded but unused therefore FAILs the page. Accepted as conservative. | Found by the independent verification pass (Sonnet 5) with a constructed page: a good face draws क्षत्रिय, a GSUB-less face is embedded unused → FAIL; the reverse (broken face draws, good face unused) is also FAIL, so no false PASS is possible. Attributing glyph runs to faces per run would need texttrace-to-xref matching that MuPDF does not expose directly; a GSUB-less face embedded on such a page is a defect waiting for the next run assigned to it, and the FAIL line names the face. | A real job where a face legitimately sits on a page drawing that script without ever drawing it — for example a field-input face that ends up in page resources — showing up as a spurious FAIL in practice. |
| 2026-09-15 | `VerifyVerdict.gates` mirrors the console: every PASS/FAIL/REVIEW/SKIP line `verify` prints is one `GateResult` with the same status, under a stable name from `verify.GATE_NAMES`; a gate that prints nothing for a job (no fields, no Arabic, no `--translations`) records nothing. Version 49 → 50. | PR #2 review F1: twelve gates recorded, eight only printed — unshaped Arabic, `/ActualText`, caption width, override markers, document metadata and its `lang` REVIEW, scaled runs, the two leak REVIEWs — so a consumer reading the verdict could see every entry PASS beside `exit_code == 1`. Printed output and exit codes are untouched (the diff is `record()` calls only); `tests.test_import_surface.VerdictCompletenessTests` counts printed gate lines against recorded entries on a real job and drives each of the eight through FAIL, PASS, REVIEW or SKIP. The version bumps because the library surface a consumer pins on changed. | A consumer that needs an entry for every gate on every job — a SKIP for "no Arabic on this page" — which would trade the line-for-line mirror for a fixed-length verdict. |
| 2026-09-16 | Every printed gate outcome carries its findings — `Finding(page, where, text)`, untruncated — in `VerifyVerdict.gates`; `--report PATH` writes `verdict.to_dict()` (schema 1, which records `fail_on_review` so an exit 1 without a FAIL gate is explained) and `pipeline.py rebuild` writes it into the work dir; `--fail-on-review` / `fail_on_review=True` exits 1 on REVIEW without FAIL; both off by default. `isolated source-script tokens: none` is PASS, not REVIEW. Every gate that is FAIL or REVIEW carries at least one finding (`tests.test_verify_report.FindingsInvariantTests`, over every kind of job the suite builds), and `run_verify` records absolute paths exactly as the CLI does. Version 50 → 51; `pdf_translate.__version__` joins the lockstep check. | `docs/BRIEF-unattended-delivery.md`: in production nobody reads the console, so a REVIEW was indistinguishable from PASS at delivery and a FAIL carried no location. The locations were already printed; the verdict now keeps them all. The "none" line was a REVIEW that fired on every job. Console output is otherwise byte-identical (`dev/probes/verdict_parity_runner.py`, nine jobs). | A consumer that needs the console's truncation mirrored in the report, or a schema change — bump `schema` rather than editing version 1 in place. |
| 2026-09-16 | Gate 19 `kinsoku`, REVIEW never FAIL: no drawn line of CJK text begins with a JIS X 4051 / JLREQ line-start member (closing brackets, hyphens, dividing punctuation, middle dots, full stops, commas, iteration marks, the prolonged sound mark, small kana, their half-width forms, the small katakana extension) or ends with an opening bracket, judged on stacks of lines grouped by geometry (one under another, up to about two and a half times the line height (three times the font size)), a stack's first and last line exempt, single-character lines and a ・ bullet that begins two or more lines exempt; one finding per line. No engine work. Test faces Noto Sans JP and SC join the fetcher (`NotoSansJP-VF.ttf`, `NotoSansSC-VF.ttf`, google/fonts builds). Version 51 → 52. | The product asked for kinsoku; measured first (`dev/probes/cjk_kinsoku_probe.py`, PyMuPDF 1.28.2, Noto Sans JP 2.04): the Story engine breaks by UAX #14 — 158 laid lines, 0 violations, against 38 + 8 for a naive breaker; 93/94 line-start and 15/16 line-end members protected. The only path that can violate is a target split by hand across source lines, which the engine never sees, so the gate reads the delivered lines. MuPDF's own blocks were tried first and split hand-split lines above about 1.6x leading (silent pass), and a hard FAIL refused correct forms (・ lists, a lone ー cell) — so stacks are geometric, the two form shapes are exempt, and the status is REVIEW because a label can look like a hand split. Console output of non-CJK jobs is byte-identical (parity runner, nine jobs). | A Story-engine release that breaks a member wrongly (the probe would show it); a real delivery where a hand-split paragraph is set looser than three times its line height (the bound would need revisiting); or a form shape that still REVIEWs in numbers — that would argue for judging only with `--translations`, where merges and hand splits are known. |
| 2026-09-16 | `--report` (and `pipeline.py rebuild`, which writes `<work>/verify_report.json`, a fixed path) does not leave a report from an earlier run behind: any file at the path is removed before the gates run, the new report is written to a temp file beside it and moved into place, and a refused write removes whatever is there and says so. Best effort — a directory that cannot be written keeps the old file, and the console says the file there does not describe this run. The exit code is unchanged. Version 52 → 53. | PR #5 review, defect 1 (the product's "section 1", received 2026-09-16): after a passing run, make the report file unwritable and run a failing document to the same path — exit 1, but the report on disk still said `exit_code 0` for the good document. A consumer reading the report saw PASS for a job that failed, attributed to a document it did not ask about. Reproduced red in `tests.test_verify_report.ReportAndPolicyTests.test_a_refused_report_write_leaves_no_stale_report`, then green. A missing report any consumer can detect; a stale one none can. | A consumer that needs the previous report kept until the new one is complete (a read-modify-write on the same path) — that would argue for swapping only at the end and never removing up front. |
| 2026-09-16 | Gate 20 `han-forms`: on a page that draws CJK text, every embedded face that drew CJK glyphs on the page (matched by name to the page's spans; an unused embedded face neither fails nor attests; a drawing face that carries no probes, or is not an embedded program, is cannot-attest for that page even when another drawing face passes) is rendered off its own font program and compared pixel for pixel with the Noto reference of the language's convention (`lang` ja → Noto Sans JP; zh, zh-Hans, zh-CN, zh-SG → Noto Sans SC) and with the other convention's, both instanced at the face's OS/2 weight; PASS when the face reads ≤ 0.02 against its own reference and ≥ 0.10 against the other on at least two of 直 骨 海, FAIL on the reverse, REVIEW otherwise — no `lang` or a `lang` that is not a language tag (a display name — REVIEW, so a wrong-format value never yields a clean verdict from a gate that did not run) (a non-CJK `/Lang` with no mapping `lang` is REVIEW, a non-CJK mapping `lang` is silence — the leak gates own it), a convention without a measured reference (zh-Hant, ko), a face without the probe glyphs, a face that differs from both references on 東 (not a Noto family), no reference directory, or an inconclusive spread. Never the nearer reference. `prepare_font` adds 直骨海東 to every CJK subset and, with `lang` and the references present, refuses a face of the other convention. References default to `tests/fonts/`, overridable with `--reference-fonts DIR` / `reference_fonts=`; instanced copies are cached beside them. Silent when `lang` is not a CJK language. Version 53 → 54. | Measured (`dev/probes/han_forms_probe.py`, `docs/BRIEF-han-forms-gate.md`, PyMuPDF 1.28.2, the google/fonts Noto Sans JP / SC variable faces): own face 0.000 on every probe; JP vs SC at equal weight 直 0.454, 骨 0.171, 海 0.212, 東 0.000; weight alone (JP 400 vs 100) 0.51–0.55, as large as region, so references must be instanced at the delivered weight; a delivery's embedded subset re-rendered reads 0.000 against its own reference, exactly as gate 18 re-probes a face, so the face is judged and not the page; Yu Gothic and Microsoft YaHei read 0.56–0.82 against both references (no reference of their family); a subset built from a target with no probe character lacks 直. The product ships both reference faces (its ruling R4) so PASS is reachable in production. Tests reproduce the table within 0.02 (`tests/test_han_forms.py`); console output of non-CJK jobs is byte-identical (parity runner, nine jobs). The whole-branch review reproduced a false FAIL (a zh → ja delivery whose stripped source face survives in the resources) and a false PASS (a probe-less Simplified Chinese subset drawing the Japanese beside an unused Japanese face) under the gate-18 enumeration; attribution by span font name fixes both (`tests/test_han_forms.py` `AttributionTests`). | A Noto release whose JP and SC builds converge on a probe (the table test would show it); a Noto delivery that reads above 0.02 against its own reference at its weight (a build mismatch between the job's face and the reference — that would argue for shipping the references with the faces, not for loosening the bound); or a target language whose convention needs a third reference (zh-Hant, ko), which adds a face to the fetcher and a measured row, not a rule change. |
| 2026-09-16 | Gate 21 `leak-cjk`: when source and output share the spaceless CJK family and the mapping's `lang` names a Han convention, every drawn line of the output is scanned for characters that cannot belong to the target — kana in a Chinese target; Han outside the target's repertoire, cp932 for Japanese, GB 2312 for Simplified Chinese, Big5 for Traditional Chinese — after NFKC folding; six or more in a line FAIL, one to five REVIEW, one `Finding(page, 'line', text)` per line; kept runs and single `--allow` tokens excluded — a line whose every tell an `--allow` token removed is counted on the PASS line and joins the kept-runs note, never cleared silently. The source's convention is read with the same tells and only measured pairs are judged (JP→SC, JP→TC, SC→JP, SC→TC, TC→SC); TC→JP, no `lang`, or an unclear source keep the old one-line REVIEW with the reason; when the tell ran, `leak-scan` records SKIP `judged by leak-cjk`, never PASS, because a line of Han both languages share is invisible to the tell, and the PASS line says so; the tell characters are printed on the console, the report carries the line. The brief's verbatim rule is not built. Version 54 → 55. | Task F said "measure first": `dev/probes/cjk_leak_tell_probe.py`, `docs/BRIEF-cjk-leak-tell.md` (PyMuPDF 1.28.2). `retypeset` refuses an omitted segment, so the leak that ships is an echo (target == source), which a verbatim rule cannot see by construction — measured on a real ja → zh-Hans delivery, it found nothing. The repertoire tell has zero false tells on eleven own-language texts including `corpus/ja_source.pdf` and 3–56 on the other language's; it found the echoed line with 14 tells; raw it also flagged a date and a name whose 年 and 郎 the source's ToUnicode had drifted to U+F98E and U+F92C — NFKC folds them. TC → JP measured one tell in 38 letters (JIS X 0208 carries most traditional forms): weak, so REVIEW; a single Traditional sentence is ambiguous to the tell (no tell for Japanese or Traditional) and gives "the source's convention is not clear". The ja-target direction is the thinner one: a one-sentence Simplified echo carries 2 tells (REVIEW), a paragraph 16 (FAIL). Into a Japanese face from google/fonts an echoed Simplified sentence never reaches verify — Noto Sans JP lacks 请 栏 东 and `retypeset`'s glyph guard refuses the job (`test_an_echoed_chinese_segment_into_the_japanese_face_is_refused_at_build`); the gate's ja-target direction covers faces that draw both scripts (a pan-CJK Noto Sans CJK). Console output of non-CJK jobs is byte-identical (parity runner, nine jobs). | A Japanese or Chinese document of the target language that carries six or more repertoire tells in one drawn line for a legitimate reason other than a kana name (that would argue for a higher bound or a wider repertoire, measured on that document); a source whose convention the tells cannot name — one character outside every repertoire (a rare name kanji such as 𠮟 or 镕) is enough today, which keeps the old REVIEW and is likelier than a mixed-region document; a tolerant `convention_of` (the convention with the fewest tells when it is clearly lowest) is a measured follow-up, not a guess; or a measured tell for TC → JP. |
| 2026-09-17 | Three gate gaps found by the first FL-150 → Japanese job run on the current skill, fixed together (v56). (1) The placement gate compares a target in a spaceless script with wrapping whitespace removed on both sides: a wrapped Japanese merge or notice puts a newline where the authored text has nothing, and every wrapped paragraph of the job came back "missing"; a Latin target still needs its spaces. (2) retypeset's canonical text layer maps a font's single-substitution alternates back to their base character, the way it already maps ligatures: Noto Sans JP swaps its digits for `locl` forms when HarfBuzz shapes a Latin run under the Japanese language tag, so `FL-150` in the notice read back as `FL-Ɍɐɋ` (digits inside a CJK run were untouched). (3) The button-chrome and caption-width gates ignore a pushbutton whose annotation flags say Hidden: a button `--hide-buttons` hid draws nothing, yet FL-150's four dead XFA buttons FAILed both gates. | In all three the file was right and the gate was wrong, which is the one failure a gate must not have. Pinned by `tests.test_cjk.WrappedCjkTargetTests`, `tests.test_cjk.LoclAlternateTests` and `tests.test_pipeline.HiddenPushbuttonTests`; measured on the job in PR #11's body. | (1) a spaceless-script target whose authored spaces the fold would hide (measured); (2) an alternate reachable from two bases with different characters — the lower code point wins today, like every drift pair; (3) a viewer that draws a Hidden pushbutton. |

| 2026-09-18 | Row 32, the terminology loop (v57). A new stage `review` (`pdf_translate/review.py`, silent, returns a `ReviewVerdict` with `to_dict()` carrying `schema` and `version`) writes `review_pairs.md` and `review_prompt.md`, ingests the reviser's `review.json`, and appends accepted terminology findings to the job's `glossary.csv`, which `qa_check --glossary` has gated on since long before this loop. `pipeline.py finish` gains `--work DIR` and refuses (exit 2) while `review.json` is absent or any finding is `open`; `--no-review` is the only way past and writes a `REVIEW no reviser:` line into the delivery. The loop is **not a gate** — no `GateResult`, no entry in `GATE_NAMES`, verify's twenty-one gates unchanged. Three rulings: (1) `finish` takes an explicit `--work` — inferring it from `FINAL.pdf`'s directory was rejected because the FL-150 delivery went to `Downloads/` while the job lived elsewhere, and a sidecar written by `rebuild` was rejected as the stale-file class PR #7 exists to fix; (2) `resolution` is a strict enum and the prose moves to `resolution_note`, with a legacy value migrated by its leading **word** (`.split()[0]` — three of the four real shapes carry a qualifier before the colon, so stopping at the colon refuses `rejected on measurement`); (3) a terminology finding without both `term` and `term_target` is reported and skipped, never guessed — `core` is the mapping key and is often a whole sentence, and the source side is the column the glossary gate matches on. **R5 is honoured by the record, not the refusal**: `finish` is packaging, not building, so a hard refusal there is a withholding by a non-build stage; `review_state.json` is therefore written beside `FINAL.pdf` on **every** run, refused or not, carrying the REVIEW line a consumer that never reads stdout can still surface. | No gate can see a wrong term of art. The first FL-150 → Japanese job exited `verify` 0 and `qa_check` with 0 errors while carrying 世帯主 for "head of household" — a status a married Japanese reader takes to mean themselves, where the US filing status requires being unmarried, on a form signed under penalty of perjury. A second reader found it after delivery. Measured against `dev/jobs/fl150-ja-2026-09-17/`: 247 cores, 13 merges, 5 overrides, 1 notice; 31 findings (1 critical, 6 major, 24 minor); 25 accepted / 6 rejected after migration; 12 terminology findings, 11 accepted. The canary's new terminology axis records the first measurement of the **reviser**: 15.29 accepted findings per 1,000 source words, and a 19% false-positive rate (6 of 31, two of them in its own top seven). Console parity verified against a `main` worktree — `dev/probes/verdict_parity_runner.py` diffs empty over its nine jobs. | A consumer that needs `finish` never to refuse (the library path and `review_state.json` already serve them; that would argue for making the CLI refusal opt-in, not for removing the record); a reviser population whose false-positive rate makes the termbase worse than nothing — the canary axis is what would show it; or a measured case where the leading-word migration mis-reads a real resolution, which would argue for refusing prose outright once no unmigrated file remains. |
| 2026-09-20 | The ratio in `scale_report.json`, in the `scaled runs (N)` console digest and against the 0.7× floor is **source-relative**: the size the page actually shows divided by the source size. For a merge that is the `MERGE_FIT` allowance (0.98, rounded to the one decimal the CSS carries) multiplied by whatever the Story engine took on top of it, not the engine's factor alone. Every merge therefore appears in the report at about 0.98× and `verify --translations` REVIEWs any job carrying a re-flowed paragraph. No version bump: the schema, keys and file are unchanged and v57 is claimed by an open PR — whoever lands this picks the bump when the stack is reconciled. | The merge path wrote `font-size:{size*0.98:.1f}px` and then reported only `insert_htmlbox`'s scale, so the two shrinks that both land on the page were reported as one. Measured on the existing PARA fixture: a 9.0 pt source printed at **8.2069 pt** (0.9119×) while the report said **0.9326×**; 0.9326 × (8.8/9) = 0.9119 exactly. The B1 recovery probe had already found five merge candidates with an *empty* report printing at 0.9778–0.9833 of source (`docs/reviews/2026-09-19-b1-recovery-probe.md`, item 2). `MergeBoxTests` asserted an empty report and called it "full size" while its own page drew 8.8 pt from a 9.0 pt source; that test now asserts the allowance and the drawn size. Rendering is untouched — the same fixture draws 8.2069 pt before and after — and both parity runners are byte-identical, though their fixtures carry no merges, so the merge console's printed numbers do change by design. `tests.test_pipeline.MergeScaleReportTests`. | A measured reason to treat the fit allowance as something other than a shrink the reader can see; or removing the allowance and letting the engine do the fitting, which changes what is drawn and needs its own rendering pass and visual review. A quieter `scaled-runs` gate is a separate ruling about the gate, not about the ratio. |

| 2026-09-18 | **E3, the consumer surface (v58). Every stage gains a silent `run_*` twin; the bare names stay exactly as they are.** `run_strip`, `run_extract`, `run_prepare_font`, `run_retypeset`, `run_verify`, `run_field_fonts`, `run_qa`, `run_review` — each returns a frozen, schema-versioned result (`pdf_translate/results.py`) and raises a typed exception on refusal; each bare name keeps its signature, its printed bytes and its return code as a thin loud wrapper. All 172 `print` sites across the package move to `logging.getLogger('pdf_translate')`; one re-entrant `console()` (`pdf_translate/_console.py`) attaches the single stdout handler at every CLI entry point, and importing the package attaches only a `NullHandler`. `PdfTranslateError` carries `console_line` and `exit_code` — the mechanism that keeps the CLIs byte-identical — plus `refusals`, every refused item by kind **with the core untruncated**, because the console's 40-character abbreviation is for a reader and a consumer matches against its own map. `run_retypeset` gains `progress`, `cancel`, `scale_report` and `resource_root`; `run_extract` requires `outdir`. `scale_report.json`'s bare list becomes `{"schema": 1, "version": …, "runs": [...]}`, written atomically, and `verify.scale_report_for` reads both shapes. C7 is NOT here (E8). | **Silencing the existing functions was measured and rejected**: `tests/test_pipeline.py` holds 177 `redirect_stdout` blocks and **128 wrap a library function directly** (`verify.verify(` 64, `retypeset.retypeset(` 55, `prepare_font` 5, `field_fonts` 2, `strip_text` 1, `propose_merges` 1). Silencing them breaks every one of those assertions and every caller written against today's shape; the twin costs two entry points per stage and breaks nothing — the branch edits **four** existing assertions in total, all of them reading `scale_report.json` as a bare list. The old silence was also a real defect: `run_verify` used `redirect_stdout(io.StringIO())`, which replaces the **process's** stdout, so one thread inside it silenced every other thread — C5, and now a red-then-green test. Two ratchets hold the console: `dev/probes/verdict_parity_runner.py` (verify, nine gate fixtures) and the new `dev/probes/cli_parity_runner.py` (twelve CLI invocations as subprocesses), both diffing empty against a v57 worktree at every commit. Suite 492 OK. | A consumer that needs one entry point per stage rather than two (that would argue for deprecating the loud names on a version boundary, not for removing the twins); a measured cost to carrying both — today it is one extra function per module and no duplicated logic, since the loud name is four lines; or a `console()` that proves insufficient for a CLI that needs interleaved stderr. |

| 2026-09-20 | **Typography design and nine-stage implementation plan approved; native execution authorized.** Work proceeds on isolated `codex/typography-preservation` from docs-only base `48dffcf`, with independent final rendering/font/layout verification. Exact font-resource associations are required: an ambiguous span records null font ID and candidate resources, never an arbitrary selected face. Raw span flags are retained as evidence but do not override consistently resolved font-program class. | Rodrigo approved the design, then the plan and its recommended execution method. A direct Noto Sans source probe reports span flags 4 despite program PANOSE sans metadata; treating that flag as authoritative would misclassify a real current fixture. The original checkout and open PRs remain separate. | An explicit operator scope change, or independently verified backend/resource evidence allowing a more precise association/classification. Ambiguity can narrow coverage but cannot justify silent style replacement. |

| 2026-09-20 | Typography font preparation takes its selected charset from bound class/role targets and validates the supplied source face and resulting subset. Relative source paths resolve beside the mapping; preparation need not require the mapping's eventual subset path to exist yet. Variable faces use actual instancing with STAT-derived names/style metadata before role validation. CJK italic/bold-italic needs a genuine suitable face; no synthesized slant is provided. | Preparation produces the file the caller subsequently names in the mapping. Existing legacy variable instances retain regular style bits at weight 700, so they cannot attest bold roles; the fontTools instancer's supported name update accompanies real outline changes. Actual regular/bold CJK outline hashes differ in the new tests. | A measured supported instancing path with equivalent consistent evidence, or genuine independently verified CJK italic resources. A missing resource cannot justify relabeling regular outlines. |

| 2026-09-20 | Typography placement measures actual selected glyph outlines with fontTools BoundsPen, checks source obstacles and proposed translated neighbors, and canonicalizes text per selected face. Distinct authored codepoints sharing one glyph in one selected face refuse rather than normalize. Canonicalization happens on a private new PDF before replacement of the destination. | The current MuPDF glyph_bbox API returned the same whole-font box for f, j, A and space; outline measurement exposes genuine italic overhang. A failing regression showed two translated neighbors could overlap despite each clearing original source boxes. Ordinary Noto space/NBSP use distinct glyphs and round-trip; an explicitly constructed shared-glyph fixture proves the ambiguous case. | A measured backend with reliable per-glyph bounds or a verified representation preserving distinct codepoints sharing a glyph. A scale exception may permit small text but never a collision, clipped glyph or silent text change. |

| 2026-09-20 | Existing Latin geometry test fixtures explicitly prefer Noto Sans Regular, with their prior fallback when it is absent. | Adding genuine typography assets made alphabetical font discovery pick Noto Sans Bold, moving an existing merge below the 0.7 floor. The same case passed with its original regular face; test asset selection, not rendering behavior, was the cause. | Deliberately remeasuring a fixture with another named face, or a fixture-specific test of font discovery. Incidental directory ordering is not a meaningful geometry requirement. |

| 2026-09-20 | Final typography may attest a subset missing only post when its surviving OS/2 and head class/weight/slant evidence exactly matches the selected face and every used glyph outline/advance matches. Identical embedded program bytes in duplicate resources may share evidence; ambiguous different programs cannot. | Actual MuPDF subsetting removes post, and field-font embedding duplicates an identical regular font. Whole-file hashes differ after subsetting, so neither names nor full hashes prove selected-face identity. Changed outlines and inconsistent surviving metadata are covered by failing/cannot-attest regressions. | A measured subset format needing another supported identity method, or independently reproduced ambiguity despite equivalent program bytes and actual glyph IDs. Missing metadata in a complete unknown font remains cannot-attest. |

| 2026-09-20 | Typography QA/review retains a record per visible occurrence; repeated-core reviewer findings require exact occurrence and source-run IDs plus the current extraction/mapping binding. Metadata-only pairs remain separate and use exact source/target without page IDs. Any new-format review validation error prevents terminology append. | Repeated wording may carry different emphasis or different number errors; core-keyed aggregation loses those facts. A failing metadata regression also showed that title/bookmark language checks must remain available. Read-only review changes no files. | A measured new review format with a more precise metadata/occurrence association. General legacy invalid-review delivery policy stays in A01. |

| 2026-09-20 | Typography CLI scaffolds bind adjacent extraction and leave language/fonts/runs/document targets unauthored. Rebuild fixes source/mapping/extraction context and rejects conflicting forwarded flags and report/input aliases before building. Lossy merge helpers refuse typography, mixed and unknown formats before writing. | A plausible legacy scaffold would silently collapse repeated occurrences; stale or conflicting context cannot describe the new artifact. Real subprocess tests inspect both status and unchanged prior files. | A separately designed lossless typography merge format or explicit broader input/output lifecycle work; neither is inferred here. |

| 2026-09-20 | The Latin command acceptance fixture uses Payment NOW and the existing source-word option. The original Pay NOW remains in occurrence-specific tests. | Measured old leak scanning has no words for the two three-letter tokens, falls back to generic Latin detection and flags a correct Spanish output. A meaningful source vocabulary tests existing verification without suppressing it. | A separately tested change to the general short same-script leak heuristic. Typography preservation cannot attest language quality. |

| 2026-09-20 | The first typography scope refuses source clipping/Form text, nonuniform transforms, stroke/transparency/blending, possible occlusion and nondefault UserUnit. Final unresolved clipping/occlusion remains REVIEW; actual distortion or changed physical page size fails. Bind effective page rectangles and retain source alpha/paint evidence. | Independent actual PDFs passed text-trace checks while hiding, distorting or enlarging content. Text and font identity alone cannot establish visible preservation. | Independently verified support for each currently refused drawing construct, with actual rasters and negative cases. Regenerate interim development extraction bindings after the added geometry evidence. |

| 2026-09-20 | Resolve final font identity against resources actually selected by page text, and embed full supplied faces for fields. | A genuine page subset and full field face can share a name but differ in bytes. Unused field resources neither invalidate nor prove page glyph identity; page subsets cannot cover arbitrary future input. | A measured alternative resource association or independently verified field-font coverage policy. Unresolved actually drawn resources still cannot attest. |

| 2026-09-20 | Exact metadata source/target review findings omit page IDs; page findings sharing metadata wording carry occurrence/run IDs. | An independent title/page collision reproduced rejection of a valid differently translated title. Separate channels retain both meanings without guessing an occurrence. | A future explicit metadata identifier format with compatibility evidence. |

| 2026-09-20 | Page typography excludes widget/comment appearance glyphs but retains their paint as possible occlusion. In-memory annotation suppression is restored before returning and its page paint order must match the complete log prefix. | An independent prefilled form falsely failed with unassigned User123. New regressions accept separate filled fields/comments and REVIEW comments covering page text. No delivered file is changed by verification. | A backend API providing equivalent annotation-free traces with verified paint-order identity, or an independently reproduced ambiguity requiring refusal. |

| 2026-09-21 | The typography capability ships as **version 59** and announces itself through an immutable `pdf_translate.MAPPING_FORMATS = ('legacy', 'typography-1')`, not through the version number. A consumer asks the engine what it can read before authoring, with `getattr(..., 'MAPPING_FORMATS', ())` so an older engine answers `()` instead of raising. The tuple describes implemented format support only: not public-release readiness, and not that any document will succeed. | v57 and v58 landed as PRs #12 and #13 on 21 September, so 59 is simply the next number; the operator chose it over 62 knowing the uncommitted v61 metadata in the original checkout renumbers when it lands. The export goes in because Task 8's supported matrix was verified end to end, which is the condition the plan set. Four tests cover it, written RED first, including an old-engine control that passed before the export existed. Verified against an actual installed copy outside the checkout, run from a neutral working directory because the working directory otherwise shadows the install: six probe cases PASS with verify 0, and the probe artifact records the task-local package path and version 59 it measured. Full suite 617 OK; both parity runners byte-identical to the frozen baseline at version 59, so the version string does not reach the console. | A measured reason to describe capability by version number instead of by format; or a second mapping format, which adds a tuple entry rather than changing the shape. Dual-host acceptance is NOT claimed: the Codex leg did not run, because `codex-cli 0.147.0` reports that its configured `gpt-6-astra` model needs a newer CLI. That is a host mismatch, not a result about this code, and it is the one check the plan asked for that is still missing. |
| 2026-09-21 | Supported Python is **3.14 only**, and CI tests exactly that: `requires-python = ">=3.14"`, the `compatibility:` line, the README and the matrix all say the same thing. The matrix keeps both operating systems, so it goes from four jobs to two. | The floor was never tested against what the work is actually built on. Every local result on this machine is Python 3.14.0 while CI ran 3.10 and 3.13, so the version all the evidence came from was the one version nobody checked. Measured before deciding: no file uses a 3.11+ construct and all 22 modules parse under 3.10 rules, so the old floor was genuine and this narrows a real promise rather than tidying a dead one. The operator chose to narrow the claim rather than keep a floor job, knowing it locks out installers on 3.10 through 3.13. | A consumer that needs an older Python, which means restoring the floor job in the same change as the metadata, not just the metadata. Adding versions back is two lines; the reason to hold them together is that a `requires-python` nobody tests is how today's Windows failure happened, in a different form. |

| 2026-09-21 | A below-the-floor scale refusal (`retypeset.py`'s legacy `overflow` list, and typography-1's `below-scale-floor` reason — not any other `PlacementError`) can opt into writing a self-contained, replayable evidence bundle: source PDF (when `original` was given), stripped PDF, segments, the authored mapping with its fonts (paths rewritten to the bundle's own copies), the structured refusal (`exc.to_dict()`), the occurrence's page/position, and its box permission. The surface is both `capture_dir=` on `run_retypeset`/`retypeset` (and `--capture-dir` on the CLI) and `PDF_TRANSLATE_CAPTURE_DIR`, the argument winning when both are set; capture is off unless one of them is. A problem writing the bundle is logged and swallowed in `capture.py`, never surfacing past the original exception. Box permission is read only from an existing authored source (a legacy merge's `box`) and is `null` with a note otherwise — typography-1 has no per-occurrence box field yet, so it is unconditionally unknown there; nothing here infers a box the caller never drew. | Three prior attempts at a real failed job came back empty — the seed corpus held only extraction verdicts and two already-fixed final jobs, the product's web app found no replayable failed bundle, and an authorized capture returned a known success (`docs/reviews/2026-09-19-b1-recovery-probe.md`) — because a refusal printed a message and exited without saving its inputs anywhere. 10 new tests (both retypeset paths; capture on writes a complete bundle, capture off writes nothing, a blocked capture directory still raises the identical `PlacementError` with the prior output byte-for-byte unchanged, and box permission is `null`+note for a plain occurrence but the actual box for an authored merge) plus 618 pre-existing pass together: full suite 628 OK in 477s; canary 15 OK in 0.8s. Both parity runners (`cli_parity_runner.py`, `verdict_parity_fixtures.py`/`verdict_parity_runner.py`) produced byte-identical stdout between this branch and `origin/main` at `d4d85be`, run twice (before and after a one-line logging fix), differing only in the expected single checkout-path stderr line. Replay was independently proven outside the test suite: a bundle written by a forced below-floor build was copied to an isolated directory after deleting the original job entirely, and rebuilding from the bundle alone raised the identical refusal — same reason (`below-scale-floor`), page (`0`), occurrence key (`'s0'`), and scale (`0.16707280636008043`) — down to the exact float. | A measured reason capture should default on, a real captured bundle that fails to replay once moved off the machine that wrote it, or a B1 design change giving typography-1 (or a wider legacy surface) a genuine per-occurrence box-permission field, which would make "always unknown" wrong for that format instead of merely incomplete. |

| 2026-09-21 | Capture also fires on a **successful** build, for any run at or under a caller-supplied `capture_below` ratio (`--capture-below`, `PDF_TRANSLATE_CAPTURE_BELOW`). Unset by default, so only a refusal captures and nothing changes. One bundle per job keyed by core, worst ratio kept; the manifest's `kind` is `refusal` or `near-floor`, and `refusal` is null on the latter. | `SCALE_MIN` is 0.7 and only a run under it refuses, so a hook keyed to our refusal is blind to a consumer holding a higher floor. Reported and then verified in `consider_ratio`: `if ratio >= SCALE_MIN: return` runs before anything reaches `overflow`, so a run at 0.72 never refuses, the build succeeds, and the consumer reverts that core itself and ships the paragraph in the source language. Their whole 0.70-0.75 band was unreachable from here. A core can be scaled more than once in a pass, so per-run bundles would multiply; the fact worth keeping is whether a core could be made to fit at all, which is its worst ratio. Verified: a near-floor bundle copied to an isolated directory, with the original job deleted, rebuilt and reported the identical ratio 0.7591 and produced a PDF - it succeeds on replay, which is the point. Full suite 635 OK; both parity runners byte-identical to origin/main. `tests.test_retypeset_capture.CaptureThresholdTests`. | A caller wanting every scaled run rather than a threshold, which is `scale_report.json` and `RetypesetResult.scaled` and needs no bundle; or evidence that per-run rather than per-core bundles are worth their volume. |
| 2026-09-21 | Every CLI and `import pdf_translate` check for `pymupdf`, `pikepdf` and `fonttools` before doing anything, name **all** that are absent, and print an install line bound to `sys.executable` plus which interpreter is running. The CLIs exit 2 with no traceback; the library raises `ImportError` with the same text. Silent when nothing is missing. The declared Python floor is stated in that message and never enforced. | Installing the plugin copies files and runs nothing, so a machine with no Python installs cleanly and fails at the first command. Reproduced: `py -3 -S scripts/pipeline.py init x.pdf` gave `ModuleNotFoundError: No module named 'pikepdf'` - one name, from a traceback, with no mention of the other two and nothing to act on. Measured that a bare `import pdf_translate` already pulls all three, so naming them together relaxes nothing: the same imports succeed and fail as before. The most common cause is an install that went to a different interpreter, which is why the message prints the running one. Enforcing the version floor was rejected: the code parses under 3.10 rules, so blocking would be this check causing the outage it exists to explain. Both parity runners are byte-identical to an origin/main worktree, because the guard prints nothing on the happy path. `tests.test_import_surface.RequirementsPreflightTests`. | A required package that is optional for some jobs, which would need per-command checking rather than one list; or a measured reason to enforce the Python floor, which means first establishing what the real floor is rather than what is declared. |
| 2026-09-21 | Every refusal list the console truncates now says how many it hid (`  … N more not shown`), and the block closes with one line naming the total and pointing a consumer at `refusals`. It does **not** name `scale_report.json`, and a refused build still writes no report. | The `scaled runs` listing has printed `… N more in scale_report.json` since v54; the two refusal blocks beside it never got the same courtesy, so a caller saw 30 entries and nothing telling them the rest existed. Measured by the consumer: it reads ~30 per round off the loud path and loops roughly `cores / 30` times - 2 rounds at normal translation length, 7 at 90% longer, 10 at three times longer, on a 247-core FL-150. Borrowing the success line's wording was rejected: it can promise a file because `write_scale_report` runs on its next statement, while a refusal raises before that call, and a report outliving a refused write is the exact failure `verify._remove_stale_report` exists to prevent - a missing report any consumer can detect, a stale one none can. Writing one here would also overwrite a previous successful run's report in a reused work dir. Both parity runners are byte-identical to an origin/main worktree, which proves no collateral console change but not the intended one: the parity fixtures never refuse more than 30, so the new lines were demonstrated separately on a 45-core refusal. `tests.test_pipeline.RefusalTruncationTests`. | A refusal path that does write a report, which would let the notice name a file again - but that needs the stale-report rule answered first, not assumed away. |

| 2026-09-22 | A01: any review loading/validation error blocks ordinary finish, even when no open findings remain after invalid findings are dropped. Finish returns 2 and records the error; explicit --no-review still delivers with disclosure. Existing final artifacts are retained but a current failed review state replaces prior success. | The v60 empty-object review reproduced review exit 2 with blocks_delivery=false and finish success. Focused rejecting/accepting cases now exercise real finish, including typography binding errors and an old-final control. Evidence: `docs/reviews/2026-09-22-a01-invalid-review-refusal.md`. This is error propagation, not a new schema or consumer enforcement policy. | A deliberately selected replacement review result model with equivalent invalid-input refusal/disclosure evidence; broader artifact invalidation is A03 and remains separately scoped. |
| 2026-09-22 | Unpublished A03 candidate: `pipeline rebuild` invalidates the default and selected schema-1 verification reports before mapping load/typesetting/verification. Current return status plus a fresh report are the evidence; prior PDFs and sidecars are not proof of current success. Unknown report files and input/output aliases are refused without deletion; inability to invalidate stops the attempt. No new report format or manifest, no change to direct `run_*` APIs or rendering. | Real success→refusal runs in legacy and typography mode retained a prior successful PDF and report because rebuild returned before calling verify. Early invalidation covers mapping errors and interruption too. `tests.test_rebuild_attempt` exercises success→failure→success, custom report switching, failure reports, aliases and exceptions. Preserving old PDFs avoids destroying a previous result. Arbitrary older custom paths remain caller-owned; concurrent jobs need separate workspaces. | A supported sequential rebuild that still advertises stale success at its default/selected report, or a valid dedicated report rejected by these checks; any future transactional output design must preserve source files and explicitly address PDF/sidecar consistency. |
| 2026-09-23 | **A cleanup pass's evidence must cover the error paths its changes moved, not only the happy paths they preserved.** A refactor that claims zero behaviour change has to say, per change, which `try`/`except`/`finally`/`close()` it added, moved or removed; where a raise can now happen relative to other side effects; and what a resource's lifetime became. Each answer needs a run on both revisions, not an argument. The mechanical half of that is now `tests.test_import_surface.LoggerCallShapeTests.test_no_except_name_shadows_a_value_read_after_the_handler`, which walks the package's ASTs and fails on any `except ... as NAME` whose NAME is a parameter of the enclosing function or is read after the handler. | Two regressions reached a reviewed branch on `simplify/upstream-cleanup` because every instrument used to accept it exercises code that works: 653 tests, byte-exact CLI parity over eleven CLIs, verdict parity over nine gate fixtures, a public-API diff, and per-commit equivalence proofs over 11,111 argv shapes, 1,369 input combinations and all 16 corpus PDFs. None of them made the new code fail. The first — an `except (OSError, ValueError) as exc` shadowing `_write_bundle`'s own `exc` parameter, so Python unbound the refusal and a best-effort capture abandoned the bundle — was found by the independent review (`docs/reviews/2026-09-22-upstream-simplify-041447e-review.md`), not by the suite. The dedicated audit that followed found a second, of the same shape and equally invisible: a `finally` dropped from `extract_segments()` leaked the source document handle whenever `image_region_warnings` raised. The new AST check reproduces the first on the commit that had it (`capture.py:238 in _write_bundle() as 'exc'`) and reports zero on `b20fadd`. | A cheaper instrument that catches the same class — a type checker or linter configured to flag it, or a coverage gate over error branches — makes the bespoke AST check redundant, though not the standing requirement to produce error-path evidence. A change that touches no exception handling, no resource lifetime and no raise ordering owes nothing here; the requirement is scoped to changes that move those, not to every diff. |

| 2026-09-19 | **B1 session scope: canvas only.** The operator requested a design canvas, then a stop for their ruling. `docs/design/B1-line-wrapping/` records proposals, not accepted placement behavior. No corpus probe, plan, build, or edits to PRs #12/#13 in this session. | The four choices affect what may move on a page. The operator explicitly reserved the ruling and the later stages; the image is schematic and claims no measured recovery. | An explicit operator ruling and authorization for the next stage. A canvas recommendation alone is not that authorization. |

| 2026-09-19 | **B1 must not add pages to the translated PDF.** The operator stated that users must not end up with more pages than the source. The canvas recommendation is exact page-count preservation, with each occurrence staying on its original page and wrapping only inside its authorized fixed box; an impossible fit refuses rather than creating a continuation page. Only the no-increase constraint is settled; the remaining placement choices await a ruling. | The user explicitly constrained the result after reviewing the canvas. Wrapping can consume available space on the same page without repaginating the document. The artifact is saved in `docs/design/B1-line-wrapping/`. | An explicit operator change to the page-count requirement. A long translation or a failed fit does not waive it. |

| 2026-09-19 | **B1 recommended direction approved by the operator.** Preserve exactly the source page count and original page assignment. Wrapping is explicit per occurrence inside a caller-authored fixed box, downward from the original first baseline, moving no neighboring content. Try source-size wrapping before shrinking within the existing 0.7× floor and explicit exceptions; an impossible fit is a build refusal, never a continuation page, clipped/omitted target, or silent source substitution. Gate 19 stays advisory REVIEW; an authored box does not confer collision safety. `docs/design/B1-line-wrapping/WEB-APP-NOTE.md` is the short consumer-session handoff. | The operator said “yes proceed with the recommended” after the page-count recommendation. This settles the design direction that the earlier rows left open. Recovery and feasibility are unmeasured, and the existing order remains corpus probe before plan before build. | An explicit operator revision informed by the corpus measurement or a demonstrated placement conflict. Unmeasured implementation difficulty does not silently waive page-count preservation. |

| 2026-09-19 | **B1 probe evidence is split by denominator and by outcome.** The 6/8 figure is successful bounded placement on authored synthetic stress cases, not customer recovery or approved-baseline compliance. All six outputs shift the first baseline; existing text-based merge selection can also choose the wrong repeated occurrence. The 16-file seed corpus has extraction verdicts, and the two available saved final translation jobs have no current refusals. Keep the approved behavior unchanged; do not size a build from this sample. `docs/reviews/2026-09-19-b1-recovery-probe.md` records the run and measurement corrections. | A created PDF is weaker evidence than complete text in the box, and that is weaker than preserving all approved layout behavior. Font span rectangles include unused ascent, so painted bounds are measured separately using isolated target pixels at 216 dpi with one-pixel quantization disclosed. Controlled stress establishes feasibility limits but supplies no representative failure rate. | Representative refused translation jobs with author-permitted boxes that establish a recovery denominator; or independently reproduced evidence that changes the measurements. A later implementation must demonstrate the approved behavior, not silently redefine recovery. |

| 2026-09-19 | **B1 next evidence step is one genuine translation attempt with complete pre-fix inputs retained.** The operator approved sending the capture request to the web-app task after its archive search found no replayable failed bundle. Use an already-authorized real form or invoice; accept success as a valid result, preserve failure evidence before retries, and never force an overflow. No B1 implementation or customer recovery-rate claim follows from this single case. Request: `docs/design/B1-line-wrapping/REAL-JOB-CAPTURE.md`. | Existing final outputs and synthetic stress cases cannot reproduce the original real fit failures. Capturing a genuine attempt preserves an inspectable example without misrepresenting synthetic wording as customer evidence. | An explicit operator change of scope or an eligible retained real failed bundle becoming available. Broader recovery estimates still require a representative sample. |

| 2026-09-19 | **Further B1 work is deferred until a genuine fit-refusal case is available.** The operator said “ok proceed with the next recommended” after the explicit recommendation to defer. Keep the approved canvas and exact page-count constraint. The capture request is fulfilled; no further capture, implementation plan, build, background monitoring, or unrelated work item is started. | The retained real-job bundle passed independent integrity and geometry checks but replayed a known successful mapping. It demonstrates no B1 recovery; another replay or forced overflow would not fill the real-failure evidence gap. Evidence: `docs/reviews/2026-09-19-b1-captured-case.md`. | A genuine unchanged failed-job bundle becoming available for measurement, with author-permitted boxes before testing wrapping, or an explicit operator change of scope. |

| 2026-09-19 | **Roadmap ownership aligned across the app and library.** Rodrigo approved option A: he owns final priorities and scope; the PDF Translator app task maintains one canonical product roadmap and owns app/service work, integration and real-job evidence; this repo owns the shared engine, reusable API, rendering and library tests. This repo's product-request inbox tracks engine requests and delivery status, not a competing product roadmap. Each cross-repository component has one implementation owner and linked state/evidence/PR/pin. The alignment was sent to the app task for documentation and confirmation, not new implementation. | Rodrigo explicitly asked to avoid collisions, contradictions and duplicate work. B1's deferral does not pause unrelated product work, and built branch work must not be mistaken for adopted library capability. | An explicit operator change to ownership or a reported responsibility conflict requiring his ruling. |

| 2026-09-19 | **The comprehensive dual-use readiness audit is evidence and recommendations, not implementation authorization.** Assess the shared engine and installable Claude/Codex skill together; distinguish reviewed v58 branch behavior, v56 default-branch state, manifest/discovery checks, deterministic tests, and unmeasured model-driven quality. Findings and reproduction evidence live in `docs/reviews/2026-09-19-public-readiness.md`. No fixes, plan, PR edits, release, or product-task assignment follows automatically; B1 remains deferred. | Rodrigo explicitly requested the broader overview. Fresh runs showed 492 passing library tests and 15 scorer tests alongside reproduced delivery/completeness defects, so a test count alone cannot stand for public readiness. The shared-engine ownership and product-source boundary remain intact. | Explicit operator authorization for a scoped next step, or newly reproduced evidence that changes a finding. A prior session claim or green subset alone does not reverse the measurements. |

| 2026-09-19 | **Audit findings enter the existing library backlog as scoped A01–A26 follow-ups.** `dev/goals/PROGRAM.md` owns their current status and closing checks; each links to its existing E/C/P/32 parent where applicable. `docs/PUBLIC-READINESS-QUICK-WINS-2026-09-19.md` is a dated shortlist, and the old HTML checklist is labeled historical. Recording an item or finishing a small docs slice does not authorize implementation or close the full behavior item. | Rodrigo requested backlog updates and a saved smallest/easiest-items document. Linking existing work prevents duplicate epics and qualifies stale completion claims while preserving history. The shared-engine/app ownership split and B1 deferral remain unchanged. | An explicit operator change of tracking location or priorities, or verified implementation/evidence that changes an individual item's state. |

| 2026-09-20 | **A04 concurrency guidance corrected; completion is partial.** Silent `run_*` functions avoid stdout redirection but do not make PyMuPDF thread-safe. Document separate processes with one job at a time per worker, process-local PDF resources, and private job/output/report paths. Keep process-isolated validation and replacement of unsupported thread-based PDF tests open under A04; app worker integration stays app-owned. The shipped skill instruction correction advances the four version sources to v59 locally under the existing version policy. | Rodrigo approved proceeding with the recommended small work. [PyMuPDF's primary guidance](https://pymupdf.readthedocs.io/en/latest/recipes-multiprocessing.html), checked 20 September, does not support multithreaded use. Existing fixture passes establish neither backend safety nor a verified app worker design. Evidence: `docs/reviews/2026-09-20-concurrency-guidance.md`. | A documented upstream threading guarantee for the supported backend versions, backed by representative independent runs, or an explicit change of scope. A successful thread fixture alone does not reverse this guidance. |

| 2026-09-20 | **A06 documentation slice: verification examples pass the mapping and check the delivered artifact.** Quickstart, hot-loop, skill and Python examples supply translations, segment context, source vocabulary and a target-script fill value. Verify again after field-font embedding or packaging; distinguish intermediate and final reports. A06 remains partial because default `rebuild` still omits mapping context unless the caller supplies it. The workflow change advances the four version sources from local v59 to v60. | Rodrigo approved the recommended next documentation item. Seven CLI examples and two Python calls, extracted from the edited documents, rejected the saved empty-target case and accepted complete-target/non-form and fillable controls: 27 expected outcomes. The unchanged default rebuild still returned 0 for the empty target. Evidence: `docs/reviews/2026-09-20-verification-examples.md`. | A verified API/default-command change may simplify explicit arguments, but any replacement must retain mapping-dependent checks and verify the exact delivery artifact. A structural pass never substitutes for visual and language review. |

| 2026-09-20 | **A15 README facts slice complete; broader support wording remains open.** Both READMEs use full unittest discovery, describe the 11 configured test fonts, install through the chosen interpreter and declared requirements, and distinguish file-presence checks from successful tests. Root navigation points to the canonical backlog and dated audit; old timing claims are replaced by the identified v58/19 September Windows measurement. Contributor test instructions are scoped to a repository checkout. The shipped README workflow correction advances the four version sources to v61 locally. | Rodrigo approved this recommended small item. The old two-module command selected 230 cases; both revised commands collect 492, matching CI's explicit test IDs, without rerunning the suite. All 11 font files are present and requirements agree with package metadata. Evidence: `docs/reviews/2026-09-20-readme-facts.md`. | Changed test/font configuration, dependency evidence or a dated replacement measurement. Broader public-support claims require their own evidence; correcting README facts alone does not establish production readiness. |

| 2026-09-20 | **Typography adoption request answered as a capability gap, preserving the product requirement.** Source serif/sans class plus meaningful within-line emphasis is not preserved end to end by the documented v54/v56 APIs or open-PR v58 adapters. The request is recorded without scheduling implementation. Source class/style runs and unambiguous occurrence association are library-owned; target/font configuration, orchestration and migration evidence are app-owned. B1 remains separately deferred. | Rodrigo authorized the app's requirements/evidence request. A library-authored fixture run against three Git snapshots yielded four occurrence IDs but discarded family/span records, flattened two mixed lines and emitted no extraction warning; their extraction-function ASTs are identical. The response and limits are in `docs/reviews/2026-09-20-typography-capability.md`. Four role files and core coverage cannot establish the stronger typography promise. | An exact delivered API and independently verified source-to-target style/occurrence evidence that meets the unchanged requirement, or an explicit operator revision of it. A future pin, a local documentation version or manual markup alone does not reverse this disposition. |

| 2026-09-20 | **Typography work advances from capability disposition to API design exploration.** Rodrigo requested proceeding with the preceding recommendation. The proposed approach adds explicit source style runs and occurrence-associated target runs while preserving legacy calls, reusing placement where suitable, exposing styles to QA/review, and reporting ambiguous/unsupported inputs. It remains a proposal: alternative replacement mapping and extraction-only diagnostics are presented for the approach ruling. No implementation, version change or app adoption is implied. | The requested behavior spans extraction, translation mapping, placement and verification/review, so a lossless representation and compatibility choice must be made before changing those consumers. The existing product typography requirement and B1 deferral remain unchanged. | Rodrigo's approach ruling; read-only source evidence exposing a conflict that requires revising the proposal. Approval of the prerequisite does not invent a final schema, accepted design or delivery pin. |

| 2026-09-20 | **Additive typography approach approved; written design awaits separate operator review.** Rodrigo approved the recommended approach: preserve legacy calls while adding source class/style runs, occurrence-associated target runs, class/role font selection and shared QA/review context. The proposed detailed scope, format and acceptance checks are saved in `docs/design/typography-preservation/DESIGN.md`, with a short review entry point beside it, in local commit `ef52e5f`. The first-scope refusals are a proposal, not permission to weaken the product typography promise. No implementation plan, renderer change, delivery pin or B1 work is authorized by the approach approval alone. | The selected approach affects interfaces used by extraction, rendering, QA and review. A concrete written design makes compatibility, unsupported cases and observable verification reviewable before planning. The architectural brainstorming workflow distinguishes approach approval from written-design approval. The library/app ownership split and B1 deferral remain intact. | Rodrigo's review of the written design, or verified source/corpus evidence requiring revisions. Future implementation and independent output evidence are required before claiming the capability is delivered. |

| 2026-09-20 | **Written typography design approved; implementation planning is the authorized next stage.** Rodrigo approved the saved design, including its additive API, explicit occurrence/style associations, first-scope refusals and unchanged page-count/baseline requirements. The nine-stage plan is saved in `docs/plans/2026-09-20-typography-preservation.md`, with a short review summary beside it. Plan details and execution method await his review; no implementation or capability declaration follows from design approval alone. | The approval responds to the concrete written design from local commit `ef52e5f`. Source inspection informed the plan, including distinct font-role keys, all mapping readers and final-artifact checks. A source-only fixture check established the expected two repeated lines/four font identities; no typography implementation was tested. The library/app ownership split, B1 deferral and existing readiness backlog remain intact. | Rodrigo requests a design revision, or implementation evidence reveals a conflict requiring a scoped revision. Plan approval and an execution-method choice are required before implementation; pushes, merges and releases retain their separate authorization. |

| 2026-09-22 | Review recommends A01 invalid-review delivery as the next bounded upstream repair, followed by separately scoped A03/A02/A06; B1 remains evidence-limited. This records a recommendation and app coordination, not a new implementation or release approval. | Fresh v60 runs reproduce all four delivery defects. The PDF Translator task agrees on A01 and owns staging/Python 3.14/exact-pin adoption; its existing v54 adapter does not call review/finish. Baseline anchoring reproduced on six synthetic cases but containment, multi-member behavior and real recovery evidence remain open. Full details: `docs/reviews/2026-09-22-yesterday-review.md`. | Rodrigo changes priorities, a fix is verified on a newer main, or a concrete consumer adoption defect justifies a different bounded repair. |

| 2026-09-22 | **A script under `dev/probes/` is frozen evidence once a review document cites its run: a cleanup pass reports findings against it and does not edit it.** The `/simplify` pass over the `codex/b1-design-canvas` candidate confirmed three quality defects in the new probes — `b1_recovery_probe.py:248` recomputes `snapshot(source)` five lines after storing it at `:243`, `b1_recovery_audit.py` repeats a span-flattening comprehension five times (`:54`, `:87`, `:119`, `:127`, `:129`) and binds three unused loop variables at `:96` — and applied none of them. Maintained code (`dev/repo/`, `pdf-translate/`, CI) stays in scope for cleanup as usual. | `docs/reviews/2026-09-19-b1-recovery-probe.md` states the audit's "reusable source is preserved unchanged as `dev/probes/b1_recovery_audit.py`", and each of the four new probes is cited with the exact command that produced a dated number; Rule 2's verify-by method only holds if the saved script is the one that ran, and a re-run writes to a fresh `--work` directory, so it cannot reproduce into the cited one. `b1_recovery_audit.py` is additionally the Rule 1 independent-verification artifact for `b1_recovery_probe.py` — the proposed cleanup was to give it the probe's own `spans()` helper, which would remove precisely the independence Rule 1 exists to buy. | A probe promoted to maintained tooling — moved out of `dev/probes/`, or given a test of its own — stops being frozen. A cited measurement superseded by a newer dated run frees the script that produced the old one. |

| 2026-09-23 | **The old `codex/b1-design-canvas` checkout is reconciled, not merged, and B1 wrapping is still not implemented.** The 18 rows above, dated 19–22 September, were ruled in that checkout: 17 were never committed and one sits in its local commit `d2b3395`. They are carried verbatim with their original dates and appended in their original order after the rows main already had. This file's order is append order, not date order; main's own rows were already out of date order. Carried onto the local branch `chore/reconcile-b1-checkout` from main `178a06f`: `.gitattributes` and the binary-integrity CI check (the three unique commits, reviewed together); the A04 concurrency guidance; the A15 README facts, re-measured on main; the backlog, product-inbox and brief statuses; the approved B1 design canvas and every dated review, probe and data file; and the separate local anchor probe `12c730c`. Dropped: the uncommitted v61 label in five files. Routed: the A06 verification-examples slice goes with the A06 rebuild fix, which rewrites the same sentences. The original checkout stays at `d2b3395` with its work untouched; the only additions there are an ignored snapshot under `runs/` and a pointer section at the top of its handover. | Main moved 54 commits past the checkout's base and assigned v59 (typography) and v60 (refusal capture), so the checkout's local v59–v61 labels named other releases, and none of its work had reached main. Merging it wholesale would have reverted main's facts (Python 3.14, 20 configured fonts, the 22 September state) and restored stale statuses. Inventory, dispositions and runs: `docs/reviews/2026-09-23-b1-checkout-reconciliation.md`. | An item in the old checkout that the report does not account for, or an operator ruling that carried text should stay unmerged. B1 itself reopens only on a genuine fit-refusal case or an explicit change of scope (the 19 September deferral row above). |

| 2026-09-23 | **A local candidate branch keeps main's version number; the next number is chosen once, when the operator authorizes integration.** The reconciliation, A02 and A06 branches all declare v60. | Numbering on parallel local branches has already collided: the old checkout's uncommitted v59/v60/v61 labels named releases that main later assigned to typography and refusal capture, and three branches bumping independently from v60 would each claim 61. PR #22 merged shipped behaviour changes (A01, A03) without a bump, so `178a06f` and `b20fadd` both say v60, and an installed plugin sees no update between them. The integration that lands these branches should therefore bump. `PROGRAM.md`'s rule (bump in the same commit as the change) still governs what lands on main. | An operator ruling to number each candidate as it is built, or a release process that needs the number before review. |

| 2026-09-23 | **A02: a translation keeps exactly the original's pages, checked on every run by the new always-on gate `page-parity`.** The gate FAILs on a missing page (`missing`, the original's number), an added page (`extra`, the translation's), or a changed media box, crop box (beyond 0.01 pt) or rotation. It records after `fill-roundtrip` with one status line; differences are indented detail lines, ten at most, and the verdict keeps them all. `compare`, `render_pages` and `pipeline.py render` show every page of both files and exit 1 when the counts differ. `finish` returns compare's code after writing everything. Matching jobs are unchanged apart from the new PASS line. Box and rotation changes are left to verify, because the images show them. There is no override. The candidate keeps v60 until integration. | Reproduced on main `178a06f` with a real strip/extract/retypeset job. A deleted page exited 0 without a mapping. An added, rotated or resized page exited 0 even with one. compare and render exited 0 showing only the common pages, because every page gate walked `min(len(o), len(j))`. After the fix all four fail by name and the equal control is unchanged. 16/16 corpus PDFs against their own stripped copies, and a retained real FL-150 delivery, give no finding. The console ratchets change by exactly one PASS line per verify run; in one verdict fixture the new gate also reports that the bad Arabic output was built on a Letter page against an A4 original (exit already 1). No stage adds or reshapes a page, B1 forbids a continuation page, and a verify FAIL stays advisory (R5). Evidence: `docs/reviews/2026-09-23-a02-page-parity.md`. | A legitimate stage that adds, removes or reshapes pages (a notice page, a normalising rewrite of boxes); that would call for an explicit, reported allowance, not a looser gate. Or a correct rebuild that moves a real source's boxes by more than 0.01 pt. |

| 2026-09-23 | **A06: a default legacy `pipeline.py rebuild` verifies against the mapping it built from.** Defined before the change. (1) When the caller forwards neither `--translations` nor `--segments`, rebuild passes `--translations DIR/translations.json --segments DIR/segments.json`, so every mapping-dependent check runs. (2) A forwarded `--translations PATH` or `--segments PATH` is honoured exactly as given, but the mapping is always supplied: `--segments` alone still gets `--translations DIR/translations.json`, and the default segments come only with the default mapping (the caller's own `--translations` keeps verify's existing rule, segments beside that mapping). (3) Only the exact tokens verify reads count as an override: `--translations=VALUE`, which verify never read, does not suppress the default. (4) There is no opt-out; structure-only checking stays available by running verify directly without `--translations`, which does not attest coverage. (5) Typography, A03's report invalidation, the build's own refusal of null or absent targets, and advisory verify FAILs are unchanged. The candidate keeps v60 until integration. | Reproduced on main `178a06f` through real rebuild subprocesses. An empty target, a whitespace-only target and an override that drops the source's `a.` marker each exited 0 with a PASS report and none of the eight mapping-dependent gates. With explicit `--translations`/`--segments` the same jobs exit 1 (`empty-targets`; `placement` and `override-markers`), so the checks exist and the default route skipped them. Typography already forwards its fixed context. The documented workflow has asked for the mapping since 20 September; a default that does less than the documentation leaves the gap to whoever forgets. Evidence: `docs/reviews/2026-09-23-a06-rebuild-mapping-checks.md`. | A legitimate legacy rebuild whose mapping-dependent checks fail on a correct output (the gate is then the bug, and is fixed there, not by skipping it); or an operator ruling that rebuild needs a documented structure-only mode, which would have to record that coverage was not checked rather than report success. |

| 2026-09-24 | **The product directs this library and assigns its work; sessions on both sides coordinate directly.** `pdf-translator` (the PDF Translator web app) sets direction and assigns work through `docs/REQUESTS-from-product.md`. This repo reports findings and proposes; it builds and tests what is assigned and starts no unassigned work. How assigned work is built stays this repo's call. Live sessions message each other when a request, the state of work or a version changes, and write every outcome into `docs/REQUESTS-from-product.md` in the same session (`AGENTS.md` Rule 4). `CLAUDE.md` now imports `AGENTS.md`, so Claude sessions load the same rules as every other agent. | Rodrigo's ruling: one owner of direction and one queue of work, so the two repos never keep competing roadmaps and nothing agreed in a chat is lost between sessions. It mirrors `pdf-translator`'s `PROJECT_CONTEXT.md` §5 ("The app directs the skill"). | Rodrigo reverses it, or the library gains a second consumer whose needs the product cannot decide for. |

| 2026-09-24 | **E12 is this library's only assigned work (priority 1 of 1). Everything else is unassigned, and Rodrigo directs library tech debt himself.** Nothing else starts here until the product sends another ID. That covers A21, the macOS test finding, A04, A05 and A14, and the remainders of E4 to E11. E12, a compact, deterministic output save, takes over E6's "one subset per document, never per page" bullet and carries C7's caller-supplied `doc_id` at its save. If E12 as written proves unbuildable or wrong for this code, the library stops and reports the measurement rather than substituting its own version. | Rodrigo's ruling of 24 September (option C), relayed by the app's session under Rule 4. The app's order is N1; then N2 (beta hardening) and N3 (Next 16); then the process-boundary migration; then N4 to N9. N1 and N3 need nothing from this library, and E4 and E5 serve the app's N6, which comes after the migration. So N2's output size is the only library behaviour on the near path. The request, with its acceptance checks, is the E12 row in `docs/REQUESTS-from-product.md`. | Rodrigo re-rules, or the product sends another assignment through `docs/REQUESTS-from-product.md`. |

| 2026-09-24 | **While E12 is blocked, the library builds its 24 September review's findings in the product's order:** R-03; R-01; the `/Rotate 90` blank page with R-64; R-02; the quick wins; then performance. E12 takes priority again the moment its six rulings arrive, and R-106 stays with E12. Group C (verify and QA verdicts) and group E (typography-1 at scale) are held. Group F (repository hygiene) and R-94 are Rodrigo's. Every item needs: a failing-then-passing test on its one-defect PDF; Rule 1 with a rendered pixel comparison for drawing, font and layout items; the wild probe with a render diff for strip changes; its own PR; a version bump and a notice to the app. | Rodrigo's ruling A, relayed by the app's session under Rule 4 (the app's `REQUEST-to-skill-review-2026-09-24`). The library proposed the findings (`docs/REVIEW-2026-09-24.md`). The app holds group C because it runs no library verify today, and group E because it has not adopted typography-1. | E12's rulings arriving (it preempts), Rodrigo re-ruling, or the product sending another assignment. |

| 2026-09-24 | **Widget text is keyed by the fully qualified field name (review R-03).** `widget_text_scaffold`, strip's `--widget-text` and `choice_exports` (which verify's `/Opt` parity reads) name a field by every `/T` up its `/Parent` chain. A key that is a full name applies to that field alone. A partial name, or its base without `[n]`, as mappings written before v62 have, applies only when it names exactly one field. A partial name that names several fields is refused (`WidgetTextError` lists their full names), and so are two keys that name one field. In `choice_exports`, a second widget of the same field gets its own `(widget n)` key. When distinct widgets share one full name but differ in the source text an entry would write, the entry is refused. That happens on a malformed form that repeats a name, or with a `/T` containing a dot that spells a nested field's name, a collision the independent verification found. Only `/Widget` annotations take part, because a comment's `/T` is its author. Captions and `--hide-buttons` keep the partial name; that is F9/row 35, which is not assigned. Version 61 → 62. | Measured on the 12 wild forms with identity widget text (every scaffold entry authored with its own source). On v61, 99 fields on 8 forms received another field's tooltip or value, and 27 of 30 USCIS barcode pages carried page 1's payload. On v62 it is 0 and 0. On those forms full names are unique while partial names collide (FL-300: 28 partial collisions, 0 full-name collisions), and PDF 32000-1 §12.7.3.2 defines the full name as the field's name. The product assigned it as priority 1 of the review (REQUEST-to-skill-review-2026-09-24). Evidence: `docs/reviews/2026-09-24-r03-widget-text-names.md`. | A form whose distinct widgets share one full name yet need different text (the specification makes them one field), or a consumer that needs one key for several fields. That would need an explicit many-fields form, not a return to partial matching. |

| 2026-09-24 | **Strip removes a text object's text and keeps its graphics state (review R-01).** Inside `BT`…`ET`, `strip_ops` drops only `BT`, `ET` and the text operators of PDF 32000-1 Table 51: showing (`Tj TJ ' "`), positioning (`Td TD Tm T*`) and text state (`Tc Tw Tz TL Tf Tr Ts`). Every other operator stays in place and in order, including colour, `gs`, line width, `q`/`Q`/`cm` and marked content. Nesting is tracked with a depth counter. Version 62 → 63. | `BT`/`ET` do not save the graphics state, so dropping whole text objects recoloured later graphics. An inventory of the 17 wild PDFs and 16 corpus fixtures (33 files, 545 streams, 39,981 text objects, 0 parse errors) found non-text operators inside text objects in 27 files and 1,192 painting operations with the wrong state, 337 visibly. The old strip differed from the fixed one by 30,709 pixels on 18 pages in 5 files: arxiv shading black, N-400 rules white, Medicare blue near-black, IRS and DS-11 fills. Two independent harnesses counted 19,933 and 19,706 non-text pixels differing from the original before the fix, on the same 18 pages, and 0 after. With this change the shipped strip matches the measured prototype on all 465 pages, and no non-text pixel differs from the original. The corpus has no `q`/`Q` inside text objects and no clip render modes (`Tr` 4–7); marked content always balances inside its text object. Clip-mode text (`Tr` 4–7) is still dropped whole, as before; a refusal for it is a separate proposal, not part of this row. Evidence: `docs/reviews/2026-09-24-r01-strip-graphics-state.md`. | A page where keeping a graphics-state operator from a text object changes a non-text pixel against the original, or a viewer that renders the kept operators differently from the original's. |

| 2026-09-24 | **Clip-mode text is left as it is and documented; strip does not refuse it.** Text drawn in a clipping render mode (`Tr` 4–7) is still dropped whole, so graphics that relied on that clip draw unclipped. No refusal is added. `references/failure-modes.md` §4 says so. | The library proposed a refusal while building R-01. The product chose to leave it (option b), relayed by the app's session under Rule 4. The app reads the library's refusals from retypeset's printed output by fixed patterns, so a new named refusal could reach an app path that does not recognise it. The 17 wild PDFs and 16 corpus fixtures contain 0 clip-mode text objects (R-01 inventory), so the risk of changing the refusal surface outweighs the benefit. | Design-heavy PDFs that use clip-mode text showing up in real jobs. Then a refusal would be revisited together with app-side support for it. Rodrigo can override. |
| 2026-09-24 | **`segments.json` records geometry in the page's unrotated space, the space of `page.get_text`; retypeset measures against the unrotated page frame.** On a `/Rotate` page, extract maps its annotation-free text page back through `page.derotation_matrix`. Width budgets and the RTL mirror use `page.rect * page.derotation_matrix`, not `page.rect`. `segments.json` gains a `geometry` key: the space marker, plus each rotated page's rotation and unrotated size. A `segments.json` without that key, on a rotated page that carries segments, is refused as a stale extraction, with a stable, documented console line. Landscape pages, whose content is counter-rotated to read upright under `/Rotate`, keep the rotated-run path and its limits. | Item 3 of the review assignment. On 2 September (`c297ac2`, v17), extract moved to a display list to keep widget values out. The helper was documented as "same shape as get_text('dict')", and `failure-modes.md` still said `/Rotate` was harmless, but the display list reports the rotated view. From v17 to v63 every run on a rotated page was misplaced. At 90 they went off the page, and at 180 they were point-reflected with retypeset and verify both exiting 0. Measured before building: a map of every extract, retypeset and verify site's space, and an end-to-end run at 0/90/180/270. The alternatives were converting at load, which cannot convert an override `x` at 90 and 270, and making the rotated view canonical, which would convert every drawing primitive and every verify reader. Both leave the library with two spaces. The app confirmed it reads no segment geometry and accepted the named stale-extraction refusal: extract and retypeset run in one job at one version, so the refusal cannot arise there. | A consumer that needs the rotated view as the stored space, or landscape `/Rotate` documents becoming common, where a reading-frame layout for rotated runs would be the next step. That was proposed and not assigned (`REQUESTS-from-product.md`, 2026-09-24). |
| 2026-09-24 | **A shrunk run is drawn at the size that fits its room, never larger than its source, with no minimum; the ratio the gate sees is that size over the source's.** The 4.0 pt lift is removed at all four legacy shrink sites: a plain run, a leader label, a right-to-left leader label and an override part. A room of 1 pt or less counts as 1 pt, so a size is never 0. Small print that needs less than 0.7× now refuses like any other run, and `allow_scale` ships it at the size that fits. | R-02, review priority 4. The lift let a source of 5.71 pt or less draw past its room onto its neighbour while reporting the lifted ratio: a 5 pt run needing 0.54× reported 0.8×, passed the floor, and verify exited 0. It drew a source under 4 pt larger than the source, reported as source size. Both contradict the 2026-09-20 row (the ratio is the size the page shows over the source size) and the module's promise that shrink-to-fit never overlaps a neighbour. A 4 pt legibility minimum can't coexist with staying inside the room: one of the two has to give. Overlap is the silent one, and a small size is reported and gated. | A product ruling on a legibility minimum, which would need a refusal (or a wrap) for runs that cannot fit above it, not a lift. |
| 2026-09-24 | **A legacy build refuses an output or scale-report path that names one of its inputs, before anything is written, as typography-1 already did; `rebuild` refuses an OUT that names the original or a work-directory input.** The inputs are the stripped PDF, `segments.json`, the mapping, the original and every resolved font. The refusal is a `MappingError` with `refusals['output_aliases']`, and `rebuild` exits 2. | R-05, the first review quick win. With a legacy mapping, `retypeset stripped.pdf segments.json translations.json translations.json` exited 0 having replaced the mapping with PDF bytes. `segments.json` and a font went the same way, and `stripped.pdf` raised a bare `ValueError`. `rebuild` never passes the original to a legacy retypeset, so `rebuild … orig.pdf orig.pdf` overwrote the original with the translation and verified it against itself, exiting 0. | Nothing expected: no caller has a reason to write its output over its own input. |
| 2026-09-24 | **Plain text that retypeset hands to the Story engine is escaped so it lands as written: `&` twice, `<` and `>` once. The four inline tags on a single-line target stay markup.** `story_text` does the escaping for the text between inline tags, for a shaped run and for a notice's text. A merge's `html` is authored HTML and is not touched. | R-06. A line that also carried `<b>`/`<i>` reached `insert_htmlbox` unescaped, so `<usuario@ejemplo.com>` vanished, `&copy;` became © and `<span>` was swallowed. Escaping once was not enough: MuPDF 1.28.2's Story engine decodes character references twice, so `&amp;copy;` still drew ©, and it did the same to a notice's text. A test renders each tricky string through the Story engine and requires it back verbatim, so a MuPDF that decodes once fails there, instead of drawing `&amp;`. | A MuPDF that decodes character references once, which the pinning test catches, or a decision to treat notice text as HTML. |
| 2026-09-24 | **Legacy retypeset canonicalizes the text layer of every role face it can have drawn with: regular, bold, italic and bold-italic, deduplicated.** | R-04, which is REVIEW-2026-09-04 F4 (candidate row 34). The rewrite got only the regular and bold faces. So an italic or bold-italic cut whose space and NBSP share a glyph drew a correct page that verify failed as non-canonical with missing targets. Arial, Times, Georgia, Verdana and Courier New do this, and so do 46 of 196 local faces. The only workaround lost the italics the docs promise. Reproduced with a Noto Sans Italic whose cmap sends U+00A0 to the space glyph: every space read as NBSP, and verify exited 1. | Nothing expected: canonicalizing a face that drew nothing is a no-op. |
| 2026-09-25 | **`field_fonts` embeds a variable face as its Regular named instance, with every axis pinned; without one, the defaults with `wght` 400. It does not refuse it.** `FieldFontsResult.instance` names the pins. | R-27. A variable face was embedded as-is. A PDF viewer draws the default outlines, so typed text came out Thin (Noto Sans JP's default is wght 100), and the variation tables took a 1 KB form to 5.9 MB. Instancing gives the weight a field wants and a 3.4 MB form, the same as a static instance to within 15 bytes. A refusal would have added a new refusal kind to a stage the app may call, for an input that has one obvious right answer. The cost is about 5 s per call for the full JP face, paid only when a variable face is passed. | A field that should render at another weight, which would need an explicit choice (an `instance=` argument), or the 5 s mattering to a service, which can pass a static face it made once. |
| 2026-09-25 | **Typography verify reads each output page's crop and each page's source field rects once, before its per-glyph loop. Its findings and their order are unchanged.** | R-47. The loop loaded the page once per glyph to read its crop. After `field_fonts` sets NeedAppearances, each load rebuilds that page's widget appearances and keeps them, so memory grew with glyphs × widgets. On an N-400-sized form (14 pages, 440 widgets), one verify made 37,618 page loads and peaked at 3.0–3.6 GB over two runs; it now makes 533 and peaks at about 170 MB. Its gate results are byte-identical on both trees, and so are those of an FL-100-sized form. The field Rects were also rebuilt per glyph, which costs time without changing anything. The Rects are now built outside the per-glyph `try` that turned an error into REVIEW. That is safe because the mapping loader rejects a malformed field rect first, and Rule 1 showed it on both trees; so no guard was added. | A MuPDF release that stops keeping rebuilt appearances would remove the memory cost, but loading once would still be correct and cheaper. A future check that needs per-glyph page state would have to read it without reloading the page. A change that lets an unvalidated field identity reach `inspect_output` would need the guard. |
| 2026-09-25 | **CI discovers its tests in every directory that holds them, and the test-font cache is keyed on the fetcher's hash and saved right after the fetch, before the suite runs. A CI-only change does not bump the version.** | R-69 and R-97. A hand-written module list left a new module unrun for about 38 hours. A literal key (`noto-fonts-v3`) is never re-saved on a hit, so both legs re-downloaded the fetcher's nine newer faces, 42,585 KB each, on every run. Saving at the end of the job would also cache the instanced faces the suite writes into `tests/fonts`: `han_forms` reuses an existing copy without checking it, so a change to the instancing code would go unexercised. No bump, as for `b39079f` and `f7326cf`: nothing an installed plugin runs has changed. | A test directory that must not run in CI would need an explicit exclusion. A fetcher whose downloads change without the file changing, for example a moving upstream URL, would need the key to carry the font hashes instead. |

Add new rows above this line, newest last. Do not renumber or reorder
existing rows when appending.
