# B1 product input request — reported result

**Result:** **Boot Spire Tech web app** replied that its retained artifacts
contain no eligible replayable real fit-refusal bundle. No case was exported.
Customer recovery remains unmeasured; the approved page-count constraint stands.

This records a data-only reply from task
`01a0bc1c-cdb2-7b93-8860-0154a450beb4`, not independent verification of that
task's files. This session did not open the product repository or its evidence
paths. [Request](../design/B1-line-wrapping/FAILED-JOB-REQUEST.md).

**Later follow-up:** an authorized single-attempt capture returned a known
successful FL-150 page-one replay. Its [independent audit](2026-09-19-b1-captured-case.md)
records the complete bundle and corrects the claimed correspondence with the
earlier school-permission jobs. It does not supply a real fit refusal.

<details>
<summary>Technical detail (skip freely)</summary>

## Reported behavioral evidence

| Retained record | Reported counts | Missing evidence |
| --- | --- | --- |
| Real-provider four-page form | 55 fitted; 94 `no_room_for_translation` blocks | Exact authored mapping paired with occurrence-level refusal and position evidence |
| Real-provider first-page form | 12 fitted; 25 `no_room_for_translation` blocks | Exact authored mapping paired with occurrence-level refusal and position evidence |

The task reports that both retain source and delivered PDFs. They are completed
outputs from an older path that retained source text, not saved library build
refusals. Wrapping permission and author-approved fixed boxes are unknown.
The counts are reported observations of that older behavior, not reproduced
library failures or B1 recoveries. The two records are not established as
independent samples; their counts must not be added into a recovery denominator.

The task also reports:

- Retained invoice/form corpus and refusal probes use synthetic wording and
  were excluded. No replayable real invoice refusal was found.
- Saved model-opus/model-fable mappings have delivered outputs. The claimed
  correspondence with this repo's two rebuilt final jobs was not independently
  checked; they do not provide original failed mappings in this response.
- Selection covered retained project `tmp`, `apps/api/tmp`, reports, and
  committed evidence. It did not select only easy cases. No replacements were
  constructed and no exports were written into this repository.

Reported paths, retained only as provenance and **not opened** here:

```text
C:\Dev\pdf-translator\.spire\clusters\tech\contracts\japanese-face\evidence\b1-retained-failure-inventory-2026-09-19.json
C:\Dev\pdf-translator\tmp\japanese-review-live\run-record.json
C:\Dev\pdf-translator\tmp\japanese-live-smoke\run-record.json
```

## Consequence for B1

The request has a response; there is no input batch awaiting replay. Neither
the synthetic 6/8 builds nor these older-path block counts establish customer
recovery. A future measurement still needs original failed inputs, occurrence
evidence, fonts, and explicitly permitted boxes. No implementation plan, build,
new corpus collection task, or product change is authorized by this report.

</details>
