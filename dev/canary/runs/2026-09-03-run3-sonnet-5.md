# Canary run 3 — Sonnet 5, 3 September 2026

Fixture, target, prompt and environment as the other two. Cost: ~162k
tokens, 74 tool calls, 11.8 minutes of working time. Skill at
`metadata.version` 45. (It also left a broad `find /` running in the
background and abandoned it after a narrower search answered the same
question; that inflates its wall clock and nothing else.)

**Score: 5 / 5** (run 2: 4 / 5 — it lost the visual axis to a merged
paragraph that shipped at 0.81× unmentioned).

| # | Axis | Score | Evidence |
|---|---|---|---|
| 1 | Identity record | **1** | `NOTES.md` opens with all four, before authoring: class, issuer (noting no district, city or state is named anywhere), parallel text with the exact search string it used, identifiers with a reason each. |
| 2 | Lookup | **1** | Searched `"Riverside Elementary School" "Field Trip Permission Slip" "Form RS-14"`, reported what surfaced (unrelated real schools, generic templates), recorded `none found` rather than asserting a match. |
| 3 | Identifiers kept | **1** | `score.py`: 4/4. Kept the school name verbatim and passed it as a phrase to `--allow`. |
| 4 | Visual pass | **1** | Both pages rendered at 110 dpi against the original, and it names what the render caught: the header paragraph shrinking to **0.81×** on the first build, fixed by widening the merge box rather than by shrinking further. That is precisely the failure it shipped unmentioned in run 2. |
| 5 | Honest delivery | **1** | Working copy stated; no reviser, said plainly; the declined letterhead merge (different sizes — a letterhead, not one sentence), the unembedded Type1 Helvetica source and the substituted OFL face, and the allowlist all disclosed. |

## Gates, measured independently

Neutral `verify.py` on `final.pdf`: **exit 1**, 14 PASS, two FAIL lines —
both `Riverside Elementary School`, which appears twice on page 1 (header
and notice) and was kept deliberately. As invoked, with the phrase
allowlist: **exit 0**. `score.py`: identifiers 4/4, `qa_check` 0 errors,
1 warning (the kept school name). 13/13 widgets, `/Opt` parity.

## Rows 24–29 and P7

Used `notices`; used `merges[].box`; two null cores under overrides;
`scale_report.json` is `[]`. Row 25 is why this run scored 5 and not 4:
the digest told it the paragraph had shrunk, and row 26 gave it the box
to fix it with.

## The one thing it missed

Its notice's bold lead-in **is not bold** — visible beside the other two
runs' page 1. It set `bold_lead: true` and pointed `fonts.bold` at the
same subset file as `fonts.regular`, so the `<b>` markup resolved to the
regular face. Nothing in the pipeline said so, and the delivery's visual
section calls the page pixel-faithful.

Scored as a pass on axis 4 all the same: the rubric asks that the run
render, look, and name something it found that way, and it did. The miss
is recorded here, and the silence is row 31 — the pipeline knows both font
paths and could say once that a bold role resolves to the regular face.
