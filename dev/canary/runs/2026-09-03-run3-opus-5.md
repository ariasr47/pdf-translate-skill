# Canary run 3 — Opus 5, 3 September 2026

Fixture: `dev/canary/fixtures/permission_form.pdf` (2 pages, regenerated).
Target: Spanish. Prompt: the one line from `dev/canary/README.md` plus the
same three environment sentences every run got. Cost: ~177k tokens, 67
tool calls, 11.7 minutes. Skill at `metadata.version` 45.

**Score: 5 / 5** (run 2: 5 / 5).

| # | Axis | Score | Evidence |
|---|---|---|---|
| 1 | Identity record | **1** | `NOTES.md` §"Identity record (written before any translation)": class (a signed, returned consent form, so compliance.md §1 applies), issuer Riverside Elementary School with the reasoning that `example.org` and a non-existent form number make it unpublished, parallel text **none (searched)**, and the five identifiers with a reason each. |
| 2 | Lookup | **1** | Searched for a Spanish issue of this document, found none, and took register from named published district forms — Lawrence County SD, Seattle Public Schools, Houston ISD — quoting the terms it lifted. |
| 3 | Identifiers kept | **1** | `score.py`: 4/4. `Attachment A`, `Schedule Q`, `Form RS-14`, the URL. `$12.00` kept; `8 1/2 por 11 pulgadas` — the measurement translated but not converted to A4. |
| 4 | Visual pass | **1** | 12 renders, 110 dpi pairs plus 200 dpi crops. `NOTES.md`'s render-pass log names two things only the render showed: the merged paragraph's first baseline sitting ~3.3 pt high because it guessed the box top, and the notice's third line reading level with the Print caption. Both fixed. |
| 5 | Honest delivery | **1** | Working copy in the required words; "no human reviser was available"; the notice, the merge box, the allowlist, the inert Print button and the locale adaptation (`Friday, May 8` → `viernes 8 de mayo`) all disclosed. `scale_report.json` is `[]` and the checklist says so rather than asserting it. |

## Gates, measured independently

Neutral `verify.py` on `permission_form_es.pdf`: **exit 1**, 14 PASS, one
FAIL — `Riverside Elementary School`, the name it kept and disclosed. As
invoked, with `--allow "Riverside Elementary School"`: **exit 0**.
`score.py`: identifiers 4/4, `qa_check` 0 errors and the one expected
warning about the kept school name. 13/13 widgets, `/Opt` parity,
`/Lang es`, `/Title` translated, caption `Imprimir`.

`score.py` again picked `field_fill_test.pdf` as the newest PDF rather
than the deliverable; the numbers above are from the deliverable.

## Rows 24–29 and P7

Used `notices` (bold lead-in, 7 pt, foot of page 1) — no script of its
own. Used `merges[].box` to give the intro paragraph a third line into
white space that already existed, so nothing shrank. Two null cores
covered by overrides. None of rows 24–29 recurred.

## What it walked into

**`prepare_font.py` does not know about row 29 or P7** (row 30). It
crashed on the null core row 29 made legal, and its charset never reads
`notices[].text`. Worked around inside the run directory with a null-free
`font_charset.json` carrying the notice text; it edited no skill file.
Reproduced independently afterwards — both halves confirmed.

Also raised, and true: `retypeset.py`'s docstring promises `dc:language`
in XMP, but a source with no XMP packet gets none created. The code is
right (it only reports `(and dc:language)` when it changed one); the
sentence is imprecise.
