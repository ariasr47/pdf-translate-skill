# Canary run 2 — Haiku 4.5, 3 September 2026 (evening of 2 September, PDT)

Fixture: `dev/canary/fixtures/permission_form.pdf` (2 pages, regenerated).
Target: Spanish. Prompt: the one line from `dev/canary/README.md`, plus
three environment sentences every model in this run got (work directory,
the venv interpreter on this Mac, no writes outside the directory; a
`DELIVERY.md` requested so the delivery is on disk).
Cost: ~87k tokens, 72 tool calls, 4.8 minutes. Skill at `metadata.version`
37 — the first canary on the shortened `SKILL.md` (P3).

**Score: 0 / 5** (run 1: 3 / 5). The file it produced is sound and
clean; the delivery around it is not the one the skill asks for.

| # | Axis | Score | Evidence |
|---|---|---|---|
| 1 | Identity record | **0** | No `NOTES.md`, no notes before the mapping. `DELIVERY.md`, written at the end, names the class ("school permission form") and treats the issuer as real (it translated the school's name), never states parallel text. |
| 2 | Lookup | **0** | Not searched, and not said. Run 1's Haiku at least wrote `none (not searched)`. |
| 3 | Identifiers kept | **0** | `score.py`: 4/4 machine-checked spans kept (`"Attachment A"`, `Schedule Q`, `Form RS-14`, the URL), `$12.00` kept — but the paper size the skill says stays verbatim, `8 1/2-by-11-inch`, became `8.5 x 11 pulgadas`. `qa_check` flagged exactly that as an error; the run never mentions `qa_check`. The axis lists the paper size by name. |
| 4 | Visual pass | **0** | Rendered both pages and built `compare.html`; the delivery says "no text drift or overflow detected" and names nothing found by looking. I looked: the page *is* clean — captions flush with their rules, list bodies aligned, leaders intact, `Imprimir` on the button. A true claim with no finding; the rubric's letter gives 0, and there was nothing to find. |
| 5 | Honest delivery | **0** | "All gates passed" while `qa_check` reports one error. No "working copy, not a certified translation" in those words; no statement about a human reviser. It does say the structure tree was removed and that a notice would be needed for official submission. |

## Gates, measured independently

`verify.py` exit 0, no gate failing. 13/13 widgets, `/Opt` export parity,
`/Lang es`, `/Title` translated, caption rewritten (`Print` → `Imprimir`),
no `allow_scale`, no `center`, no `right`. `qa_check.py`: 1 error (numbers
in the paper-size line), 1 length warning.

## What this says about the skill, and what it does not

- Rows 18, 19 and 23 hold on the fixture that first exposed 18: the
  signature and date captions sit on their rules (no `center` this time),
  and the five list bodies line up with the source.
- The missing statements all live in steps the shorter `SKILL.md` did
  **not** move: the identity record in step 1, "working copy … in those
  words" and the reviser sentence in step 8. The session was 87k tokens
  with no compaction, so this is the model, not the length.
- It translated the issuer's name (`Escuela Primaria Riverside`), which
  the skill allows for a name only referred to, and disclosed it. It did
  not add a notice, judging the form internal — the same call as run 1.

Haiku-class remains what the program says it is: a canary for the skill
text, not an author. This run found no skill defect.
