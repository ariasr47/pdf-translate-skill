# Canary run 4 — Opus 5.5, 25 September 2026

Fixture: `dev/canary/fixtures/permission_form.pdf`, 2 pages, regenerated
from a frozen copy of the skill (sha256 `c1a357be…e0e2`). Target: Spanish,
as in runs 2 and 3.

The prompt was the one in `dev/canary/README.md`, word for word, plus three
environment sentences:
- the run directory, which existed and was empty;
- the venv interpreter on this Mac;
- no writes outside the run directory.

The skill was a `git archive` of `4793f38`, at `metadata.version` **77**: the
tip of PRs #42 to #48, not `main` (v71).

The model was Opus 5.5 (`claude-opus-5-5`), run as a subagent of a session
on the same model. Cost: ~268k tokens, 95 tool calls, 14.7 minutes.

**Score: 5 / 5.**

| # | Axis | Score | Evidence |
|---|---|---|---|
| 1 | Identity record | **1** | `NOTES.md` opens "Written 2026-09-25, before any translation was authored". It gives all five facts. **Class:** a US elementary-school field-trip permission slip. **Issuer:** Riverside Elementary School as printed, unknown beyond that, reasoned from the example.org URL. **Parallel text:** none, searched. **Identifiers:** filled after extract and before authoring. **Terms of art:** a table with sources. |
| 2 | Lookup | **1** | It searched on 2026-09-25 for a Spanish version of this document and found none, and says so. It took register from published district forms: it read Duplin County Schools' Spanish form in full, and saw Sequoia UHSD's, Samueli Academy's and SEV Veracruz's. Two terms had no established rendering, "cuota" for the trip fee and the grade labels, and it flags both for the reviser. |
| 3 | Identifiers kept | **1** | `score.py`: 4/4 (`Attachment A`, `Schedule Q`, `Form RS-14`, the URL). `$12.00` and `$` are kept. The paper size is bilingual: "papel tamaño carta (8 1/2-by-11-inch)". |
| 4 | Visual pass | **1** | Five rounds rendered beside the original, each naming what the render showed. The merged paragraph came out at 0.80×, so it widened the box. The line then broke inside the date, so it moved the break. "Cantidad adjunta" touched its dot leaders; "Monto adjunto" left an 11 pt gap; "Importe adjunto" ends 3.5 pt short, where the source leaves 2.8 pt. |
| 5 | Honest delivery | **1** | "Not a certified translation" and "No human reviser was available", packaged with `--no-review`. It discloses the notice and its wording, both allowlisted phrases and why, the merge box, the inert Print button, the absent tagging, encryption and `/Perms`, and the one scaled run (0.98×, the merge allowance). It also names what it could not check: the dropdown display in a conforming viewer. |

## Gates, measured independently

- **Neutral `verify.py`** on `final.pdf`, without an allowlist: exit 1. The
  one FAIL is the school name it kept and disclosed, as with Opus 5 and
  Sonnet 5 in run 3.
- **As invoked**, with `--allow` for the school name and the English
  `/Title` it quotes in the notice: exit 0, 15 PASS and 2 REVIEW. The
  REVIEWs are the kept identifiers and the merge at 0.98×.
- **`score.py`:** exit 0. Identifiers 4/4. `qa_check` 0 errors and 1
  warning, the kept school name. Terminology not measured, since there is
  no `review.json`.
- **Fields:** 13 in, 13 out, same names, types and rects. `/Opt` exports
  unchanged, and the Print caption rewritten as "Imprimir".

**Not scored:** five rebuild rounds. `qa_check`'s one ERROR, numbers doubled
in the paper size, was fixed before the first build. No glossary was
invented.

## What it found in the skill

It reported four problems in `NOTES.md` and changed no skill file. Each was
checked here afterwards.

1. **`pipeline.py init --widget-text` overwrites the authored file:
   reproduced.** Init applies the widget text, then extract writes a fresh
   scaffold to `WORK/widget_text.json`. An author who fills that scaffold
   in place, then runs init again as `SKILL.md` directs, loses the targets.
   On `corpus/choice_fields.pdf`, 5 authored targets became 5 nulls. The
   model kept a copy as `widget_text_es.json`.
2. **`/Lang` loses its region: reproduced.** PyMuPDF's `set_language`
   writes `es` for `es-US` and `pt` for `pt-BR`, while keeping `zh-Hans`.
   Retypeset still logs `/Lang -> es-US`. Verify's metadata gate compares
   the primary subtag, so it passes.
3. **The review prompt's identity comes from an undocumented `document`
   block: confirmed in code.** `review.py:665` reads
   `translations.get('document')`, and `references/translations-format.md`
   does not mention it.
4. **The ink ratio moved between builds of pixel-identical pages: not
   reproduced here.** Page 1 read 1.18 on `out.pdf` and 1.32 on
   `final.pdf`. The model attributes it to verify's fill round-trip; widget
   appearances being rebuilt under `NeedAppearances` could also explain it.
   Recorded, not diagnosed.

**Its footprint outside the run directory, which it disclosed:**
- a `__pycache__` in the skill copy, which it deleted;
- an empty file in `runs/` from a mistyped redirect, which it removed;
- the scorer's own temporary file.

It printed its scoring report to the terminal rather than write outside the
run directory.

The three reproduced findings are filed as proposals in
`docs/REQUESTS-from-product.md`, dated 25 September.
