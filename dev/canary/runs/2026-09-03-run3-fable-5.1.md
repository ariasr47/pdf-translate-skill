# Canary run 3 — Fable 5.1, 3 September 2026

Fixture, target, prompt and environment as the other two. Cost: ~190k
tokens, 52 tool calls, 11.8 minutes. Skill at `metadata.version` 45.

**Score: 5 / 5** (run 2: 5 / 5 on this fixture, and 5 / 5 on the real
FL-100).

| # | Axis | Score | Evidence |
|---|---|---|---|
| 1 | Identity record | **1** | `NOTES.md` §"Identity record (written before any translation was authored)": class, issuer treated as fictional for lookup because `example.org`, `Form RS-14` and `Schedule Q` belong to no public catalogue, parallel text **none** with the date of the search, identifiers with the disposition of each. |
| 2 | Lookup | **1** | Named three published district forms it took register from — Nyack, Poudre, PLUSD — and listed the terms of art lifted from that class (*permiso para excursión escolar*, *contacto de emergencia*, *kínder*). |
| 3 | Identifiers kept | **1** | `score.py`: 4/4. `Schedule Q` and `Form RS-14` rendered bilingually (*el formulario Schedule Q*, *el Form RS-14*), which the rubric allows. |
| 4 | Visual pass | **1** | Three builds, full-page passes at 110 dpi plus 300 dpi close-ups. Two defects found by eye and fixed: the notice's bold lead-in rendering regular (added a real bold face) and dot leaders running flush against *Cantidad adjunta* (trailing space on the label). |
| 5 | Honest delivery | **1** | Working copy; no reviser, and the need for a qualified EN→ES one named; what it could *not* do listed separately — the dropdown never opened in a real viewer because Chrome's PDF pane could not be driven, `/Lang` left as `es` rather than `es-US` because MuPDF keeps the primary subtag. |

## Gates, measured independently

Neutral `verify.py` on `final.pdf`: **exit 0**, 15 PASS, no FAIL and no
allowlist needed. `score.py`: identifiers 4/4, `qa_check` **0 errors, 0
warnings** — the only clean sheet of the three. 13/13 widgets, `/Opt`
parity byte-identical, all 7 tooltips translated, caption `Imprimir`
fitting at 64.98 of 66 pt.

## The judgment that differs from the other two

It translated the issuer's name — *Escuela Primaria Riverside* — where
Opus and Sonnet kept `Riverside Elementary School` verbatim and
allowlisted it. That is why it needed no allowlist. It is defensible: the
notice quotes the English title in full, so a parent can still name the
form at the office, and the school's name is not one of the five
write/find/say spans the rubric checks. Recorded as a difference, not a
deduction.

## What it walked into

The same `prepare_font.py` gap Opus hit, from the other side (row 30):
the charset never reads `notices[].text`, so its notice's curly quotes
were not in the subset and the build failed the glyph check. It worked
around it with straight quotes and flagged the defect rather than editing
a skill file.

It also hit the bold-lead silence (row 31) but caught it by eye and fixed
it with a real bold face — the same defect Sonnet shipped.
