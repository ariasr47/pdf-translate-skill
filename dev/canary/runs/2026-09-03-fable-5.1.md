# Canary run 2 — Fable 5.1, 3 September 2026 (evening of 2 September, PDT)

Fixture: `dev/canary/fixtures/permission_form.pdf` (2 pages, regenerated).
Target: Spanish. Prompt: the one line from `dev/canary/README.md`, plus the
same three environment sentences every model got this run (work directory,
the venv interpreter on this Mac, no writes outside the directory; a
`DELIVERY.md` requested). Cost: ~207k tokens, 49 tool calls, 14.6 minutes
(run 1: 209k, 55 calls, 14 minutes). Skill at `metadata.version` 37 — the
shortened `SKILL.md` (P3).

**Score: 5 / 5** (run 1: 5 / 5). Every claim I tested was true, and it
handed the two decisions that are not its own back to the requester.

| # | Axis | Score | Evidence |
|---|---|---|---|
| 1 | Identity record | **1** | `NOTES.md` before any translation: class (a consent the parent signs and submits, so a notice belongs on page 1), issuer as printed and "not locatable" (reserved example domain), parallel text *searched, none*, identifiers listed by what the parent must write, find or keep — including the paper size and the `$`. |
| 2 | Lookup | **1** | Searched for this document (none) and for the class: named the district slips it tried, read Poudre SD's Spanish form in full and took its terms (`Nombre del Estudiante`, `Contacto de Emergencia`, `Firma del Padre/Tutor`), and the Sequoia and Jefferson Parish titles for the form title. |
| 3 | Identifiers kept | **1** | `score.py`: 4/4. Also `8 1/2 por 11 pulgadas` (numbers verbatim, not converted), `$12.00` and the `$` tail, and the school name kept in English as a name the parent says and finds. |
| 4 | Visual pass | **1** | Named things found by looking: the dot leaders after `Cantidad adjunta` start 1 pt after the label where the original had 3 pt (seen at 300 dpi); the dropdown showing `First` in MuPDF and `Primero` in Chrome; typed accented Spanish rendering from the embedded font in every field; the notice's three 8 pt lines at the foot. |
| 5 | Honest delivery | **1** | "This is a translated working copy … not a certified translation" in those words; "No human reviser read this", with `review.json` labelled a model first pass, not qualified in the pair, five minor items for a reviser; the notice decision and the dead Print button handed to the school; tags, encryption, certification and the 13 KB → 446 KB font embed all stated. |

## Gates, measured independently

`verify.py` on `final.pdf` as the run invoked it (`--allow "Riverside
Elementary School" --translations --source-words-from`): **exit 0**, every
gate PASS, and the row-20 note: *2 source-language run(s) kept as the
original /Title or an allowlisted phrase: Field Trip Permission Slip;
Riverside Elementary School*. Invoked neutrally, the title is still kept
and the only FAIL is the school name the run disclosed and allowlisted as
a phrase — the friction run 1 recorded is now one word on the command
line, and the notice no longer trips the scan. 13/13 widgets; `/Opt`
export parity; `/Lang es-US`; `/Title` translated; `Imprimir` fits with
about 1 pt to spare. `qa_check`: 0 errors, 1 warning (the school name
mapped to itself, deliberate). `score.py` picked the run's fill-test file
as the newest PDF, which is why its own table shows one failing gate; on
`final.pdf` the numbers above stand.

## What it walked into

1. **The notice channel, again.** Like run 1, it wrote its own
   `add_notice.py` to draw the compliance notice on `stripped.pdf` before
   retypeset (this time before, so the canonical-layer rewrite covers it —
   a cleaner trick than run 1's). Two runs out of two on a form that needs
   a notice have had to build the tool. Lane B candidate **P7** now has a
   second measurement.
2. **A merge that needs one more line than the source.** The two-line
   intro is a `merge-candidate`; the Spanish needs three lines in the
   union box of the two source lines and would scale below 0.7×, so it
   declined the merge and translated line by line, breaking at a clause —
   the same workaround Opus used in run 1, and the same worse document:
   the source's wrap points hard-coded into the target. There is empty
   space below the paragraph on this page. A merge that may grow by a
   line when the room below is empty is a measured candidate, not a row
   yet.
3. **Leaders start 1 pt after the label** where the source had 3 pt.
   Cosmetic, seen only at 300 dpi; the leader refill anchors right and
   fills from the label's end. Recorded, not a row.

## Also worth recording

- Ran `qa_check.py`, reworded to keep every run at 1.0× (no
  `allow_scale`), and checked the built file in a real viewer (Chrome),
  which is where the dropdown's translated display half is visible.
- Kept the school name in English and said why; localized the deadline's
  date order and said why.
- It invented no glossary. It downloaded Noto Sans (OFL) into its work
  directory, as in run 1.
- It noticed `dev/canary/last-score.json` changing under it while I was
  scoring Haiku, and left it alone.
